# PixBots-G: Master Status Document (Active Work & Backlog)

This document tracks the active implementation targets, backlog, and design expansions for PixBots-G. (All shipped and completed features have been cleared from this tracker - see the short "Recently Shipped" section at the bottom only for orientation context.)

---

## 1. Lore & Vision Context
**The Core Fantasy:** You are playing *PixBots*, a wildly popular tabletop miniatures game at Frank's local hobby shop. The scale is 20mm miniatures fighting on a 4x8ft tabletop mat (1mm = 5px, 1 inch = 127px).
**The Goal:** Survive as many waves as possible against Frank's proprietary AI Director, which evolves and breeds new rival profiles to counter your strategies.

**Story (Canon):** The shop runs a standing competition: defeat the AI and earn merchandise. The Black Market is the shop's glass case; the War Room is your opponent studying your plays.

**Open worldbuilding thread (not yet authored, direction only):** hints that the world outside the shop is a dystopian collapse Frank never comments on directly - see STORY_SCRIPT.md's "Worldbuilding thread: dystopian hints" section for seed ideas (Neighborhood Watch sweeps for unauthorized electronics/thermal signatures/unregistered materials) and the "The Eye" challenge concept below that gives it a first concrete gameplay hook.

**Standing design rulings (locked):**
- Purely procedural 2D visuals; sprite PNGs are optional *overrides*, never requirements.
- Every Champion Card PNG carries the full buildout (ghost + blueprint duality).
- Explosions are payload-only (no flight identity). Skill trees are QoL, not power.
- Corporate Sponsorships bias loot via **weights, never hard filters** - no build path can be bricked.
- Pixelbots 2 (godot-3d) shares gameplay rules via Node-free RefCounted/static classes, targeting an engine-agnostic `pixbots_core` Rust crate long-term.

---

## 1a. Pixelbots 2: The 3D Vision (captured 2026-07-20, not active work)

This is the actual long-term destination the `pixbots_core` split above is aimed at. Recorded here as durable design intent, not a task queue - **PB1 stays the priority**, and PB1 only picks up changes from this vision when they're cheap/natural to do anyway (e.g. work already being done for other reasons that happens to also serve PB2). Nothing below should be read as "go build this now."

**The core idea:** the hex grid isn't 2D forever - it's the 2D unfolding of a **3D cube grid**. Each hex a player places today is a face-projection of a cube; when the game goes 3D, that same build data describes a genuine 3D voxel structure. Garage UX changes accordingly: selecting a limb zooms the camera into a 3D model of that limb, and the player sees/edits columns and rows of cubes directly on the model - build logic and rules stay hex-based (so PB1's balance, routing, and solver code ports largely as-is), but the *view* is a real 3D shape, not a flat schematic.

**Why this is exciting, in the user's own words:** the hex-grid shape rules already produce "weird long limbed" torsos and asymmetric builds in 2D - wrapped up into 3D cube volumes, those same shapes read as genuinely strange, cool silhouettes rather than just an odd flat outline. The build-in-hex/view-in-3D duality is the whole hook: familiar hex-grid engineering, alien-looking robots.

**What this implies architecturally (informs, doesn't mandate, PB1 work):**
- The hex-grid simulation (packet routing, tile process_energy, capture/store semantics - i.e. everything `hexgrid_sim.rs`/`RustGridSim.gd` now cover) is exactly the "gameplay rules" layer meant to become the engine-agnostic `pixbots_core` crate. Every tile ported to Rust in PB1 is (long-term) a tile PB2 doesn't have to re-implement - this is the concrete throughline from "port tiles to Rust for PB1 performance" to "build the PB2 foundation." They're the same work, not competing priorities.
- Shape-generation code (`ComponentEquipment.generate_shape`/`generate_procedural_shape`) is the other natural PB2 seed - it already reasons about hex geometry as pure data (no Godot Node coupling in the algorithm itself), which is what a cube-projection layer would need to consume.
- The engine question is open: Godot supports 3D natively (no engine swap forced), but a 3D-heavy sequel is also the natural point to revisit whether Godot remains the right fit vs. something with more mature 3D tooling - not a decision to make now, just flagged as a real fork in the road when PB2 actually starts.

**Explicit non-goal for PB1:** PB1 does not need a 3D renderer, a cube-projection layer, or any camera/zoom-into-limb UX. Its job is only to keep the simulation/shape-generation layers clean and engine-agnostic where that's low-cost, so the eventual extraction is easier.

**Axial-to-cube axis mapping (raised 2026-07-27, purely conceptual, not to-do yet):** the user's own framing of "rolling up" a hex line into a line of cubes - a single straight line of hexes stays a straight line of cubes, but the existing "weird long-limbed dog-leg" shapes (already produced by the current hex shape-generation budget/spoke rules) would read as genuinely strange 3D silhouettes once every bend becomes a real turn in 3D space rather than a flat direction change. Tentative axis idea floated: hex E/W -> world X, NE/SW -> world Y, SE/NW -> world Z (an axial-to-cube-diagonal mapping, since a 2D hex grid has 3 natural axes/6 directions, matching a cube's 3 axes/6 faces one-to-one) - plausible on its own terms but genuinely untested; needs real validation once PB2 work actually starts, not now.

---

## 2. Execution Queue (in order)

Garage QoL first (felt immediately), then small gameplay wins, then progression systems, then the big engine lift, then the feature batch.

1. **Data-driven tiles** - shipped. Every one of the 24 tile scripts now reads its balance numbers (weight, charge/damage multipliers, per-rarity power/duration/interval curves) from `res://tiles/<TileType>/stats.json` via the new `TileStatsRegistry` autoload, falling back to the original hardcoded value if a file/key is missing - purely additive, verified byte-identical to prior behavior via `DataDrivenTilesCheck.gd` across all 24 types and all 5 rarities. Optional sprite PNG override per tile folder is a natural next step (not yet wired) for the full custom-tile-mod staging; procedural visuals remain the default per the locked ruling either way.
2. **Late-game progression** (in order):
   - *Overclocking (prestige):* at max component level, safely un-equips Mod Chips back to inventory, drops Chip Capacity until re-upgraded; grants permanent mass reduction or +1 hex.
   - *Chip Splicing:* merge two Mythic chips into one "Corrupted" dual-effect chip carrying a severe drawback from a large pool.
   - *Nemesis Bounties:* the Director breeds a boss from the player's own genetics (BossEvolution) to counter their build; drops unique inverted-synergy Nemesis Parts.
3. ~~Rust projectile physics port (Phase 3)~~ - **shipped 2026-07-27.** Turned out a prior antigravity session (2026-07-24, plan at `C:\Users\Utility\.claude\plans\effervescent-exploring-pine.md`) had already built Days 1-2 (Rust `query_hits` swept-circle-vs-segment test, the `ProjectileBroadphase.gd` autoload batching registration/reports/dispatch, `PartHitbox`/obstacle/husk group+radius registration, and a parity check) but never wired it into `Projectile.gd` itself - this session found that plan, verified Days 1-2 with the existing parity check (passed clean), and executed Day 3: `Projectile.gd` now `extends Node2D` (was `Area2D`), no more per-projectile `CollisionShape2D`/physics-server registration - every projectile reports its swept movement segment to `ProjectileBroadphase` each tick, which runs ONE batched Rust call for all live projectiles against every live `PartHitbox`/obstacle, then dispatches hits into the unchanged `_handle_hit()`. Fixed the 3 call sites that touched the old Area2D API (`MortarShell.gd`, `HitDecayCheck.gd`; `MagnetSystem.gd` needed no change). Verified: parity check (Rust == GDScript fallback, exact pair match), `HitDecayCheck`, `ProjectileMaterialSharingCheck` all pass; full headless smoke test clean, no stray processes. Real playtest evidence (2026-07-27 stress video, wave 12, 495 live shots, 34 enemies): collision pairs stayed at 9 - broadphase is confirmed NOT the bottleneck anymore, exactly as intended. **Not done manually**: the plan's own Day 4 mechanic checklist (pierce multi-hit, lightning chain re-targeting, poison mine detonation, Mortar direct-fire + lightning re-arm, Magnet repel reflection, no new tunneling) - recommend a real playtest pass to confirm before calling Phase 3 fully closed out.
4. **Rust Phase 4 (captured 2026-07-27, scoped from real evidence, not started):** the same stress-test video that confirmed Phase 3 also surfaced the NEXT bottleneck - `shoot` (896ms/sec) dominated `projectile_physics` (231ms/sec) in FpsCounter's real in-session overlay, the opposite of what was assumed going in. Two follow-up diagnostics (both built and run this session) redirected the target twice:
   - **`ProjectileConstructCostDiagnostic.gd`** (new, reads `Mech._perf_shoot_usec`/`HexTile._perf_projectile_construct_usec` DIRECTLY - real accumulated counters from real `_shoot()`/`_fire_combined_projectile()` calls, no wall-clock diffing, so no warmup-noise risk): of 600 real fired shots, projectile construction (`Projectile._ready()`/`_build_visuals()`) was only ~33% of shoot cost - the merge/pattern/consolidation packet math in `HexTile._fire_combined_projectile` was the bigger share at ~67%. **Shipped two passes** (2026-07-27), both verified via `TestRangeCheck`/`TileConfigCheck`/`DataDrivenTilesCheck` PASS and re-measured via `ProjectileConstructCostDiagnostic.gd`'s real accumulated counters (no wall-clock noise risk): (1) stripped ~7 always-true `"field" in proj/mech/packet` runtime existence checks plus `.get()`/`.set()` dynamic dispatch in `_fire_combined_projectile` (`proj`/`mech`/`packet` provably always have every field touched there - `_ProjectileClass` is a `const` preload, never polymorphic) for direct member access - 38.82ms -> 37.13ms across 600 real `_shoot()` calls (~4%); (2) added `EnergyPacket.add_synergies_batch()` (syncs magnitude/synergy proportions ONCE per merge instead of once per synergy key - `_sync_synergies_to_magnitude()` is O(n) in synergy count, so the old per-key `add_synergy()` loop was O(n*k) of redundant resyncing) and used it at `_fire_combined_projectile`'s two consolidation-buffer merge loops AND the same copy-pasted pattern found in `ResonatorTile.gd`/`SplitterTile.gd`/`brands/PrimeCircuitTile.gd`'s remnant-magnitude folding (real grid-simulation hot paths too, not just shoot) - 37.13ms -> 36.82ms (~1%). **Honest verdict**: real, permanent, safe wins, but only ~5% combined (~0.061ms/shot) - this hot path just isn't that expensive per-call. The video's 1-2fps collapse is more likely aggregate call VOLUME (hundreds of shots/sec) and/or rendering overdraw (heavy ADD-blended transparent glow stacking, "1727 draws") than any single fixable line here. Further GDScript micro-optimization on this specific region has hit diminishing returns - next real win needs either a genuine Rust port of this whole region (uncertain payoff now that GDScript measures this lean) or redirecting to a different Phase 4 candidate (homing/vortex batching, sight raycasts, MechRenderer polygon union - all still unstarted) or the rendering/overdraw angle, not yet investigated at all.
   - **`CharacterBodyExistenceCostDiagnostic.gd`** (new, warmup-corrected re-test of a prior session's claim that bare `CharacterBody2D`+`CollisionShape2D` registration costs ~350ms/tick at 60 mechs just by existing): the corrected test reproduced the same large cost even after a 20-frame warmup, BUT tracing it further found the real cause - `Mech._ready()` unconditionally builds a full 6-slot starter loadout (`ComponentEquipment.create_starter_*` x6) AND calls `MechRenderer._rebuild_visuals()` (including the `Geometry2D.merge_polygons` silhouette computation already flagged elsewhere as a real spawn-stutter cost) for EVERY mech instance, regardless of whether the caller intends it to stay "bare" - so neither this test nor the original session's diagnostic ever actually isolated bare physics-body cost; both were unknowingly measuring full starter-body construction + first-time silhouette computation instead. **Corrected finding, not a new mystery**: this converges with the already-known, already-partially-mitigated (`_component_polygon_cache`) MechRenderer polygon-union cost - a genuinely isolated bare-CharacterBody2D test (skip `equip_component` entirely) is still open if the question matters enough to pursue further, but is lower priority now that the real cost has a known owner.
   - **Rust-portable candidates identified but not yet built**, in priority order: (1) packet/pattern math in `_fire_combined_projectile` (see above - now the best-evidenced target), (2) homing-target search + Vortex pull-scan batching in `Projectile.gd` (reuses the exact `ProjectileBroadphase` register/report/batch/dispatch plumbing Phase 3 just proved out - `_find_homing_target()`/`_pull_nearby_items()` still do individual `direct_space_state.intersect_shape()` calls per-projectile), (3) sight/search raycasts (`SightAndSearch.gd:79,196` - confirmed per-mech-per-tick `intersect_ray` calls via `direct_space_state`, same dispatch-overhead category), (4) `MechRenderer`'s `Geometry2D.merge_polygons` silhouette union (already cached for repeat shapes, but every genuinely new shape - fresh boss/rival rolls - still pays full Godot-side cost on a cache miss; natural extension of the existing `part_rasterizer.rs`).
   - **Also shipped this session as a quick, well-evidenced, low-risk win** (not a Rust port): `MechStatusBars.gd` now gates its `_draw()`/`queue_redraw()` on true on-screen visibility (`VisibleOnScreenNotifier2D`, same pattern `Projectile.gd` already used for off-screen culling) instead of redrawing unconditionally on every hp/shield change - deliberately screen-visibility-based, not a distance radius, so it doesn't affect long-range Kinetic engagement of on-screen-but-distant targets.
5. **Feature batch:** Bot League auto-pilot spectating (entered via Frank's exhibition table), Tactical Puzzle Challenges (Frank's chess-style JSON scenarios, restricted parts, strict timers), Shop events (wager waves first; Shop Cat flagship later), shop-framing UI paradigm pass across all menus. Champion Card art compositing the real mech sprite: shipped. Zero code exists yet for Bot League/Puzzle Challenges/Wager Waves/Shop Cat - fully greenfield.
6. **Data-driven Map/Campaign Editor + Multiplayer** (captured 2026-07-27, design-only, not started): a standalone editor tool for building maps/campaign content that does NOT need updating every time game code changes - editor and game should share one common index/schema (tile types, stats, biome/terrain definitions, etc.) that the editor reads live from the game's own data (mirroring how `TileStatsRegistry`/`res://tiles/<TileType>/stats.json` already decouples balance data from code) rather than hardcoding a duplicate schema in the editor. Synchronous multiplayer (GodotSteam, see Future Expansions below) is a related long-term wishlist item, tracked separately there.

---

## 3. Backlog & Improvements

### Bugs & Code Health
- **God-Class Aftercare (done):** `Mech.gd` audited at ~3000 lines. The 5 pre-existing splits (`BossBrain`, `StatusEffectRunner`, `PlayerController`, `MagnetSystem`, `SightAndSearch`) were confirmed genuinely lazy-constructed and cleanly delegated - never the problem. All three "equippable ability" cuts are done: `CloakSystem` (`b3adf92`), `JammerModuleSystem` (`fedc6ae`), `HealBeaconSystem` (`4c17922`) - each verified with zero regressions across the full check suite. The former 394-line `_recalculate_grid` was also broken up: unlike the ability cuts, it's the central orchestrator touching ~25 fields across every other subsystem and is called externally from `GarageSimulationRunner.gd`/`GarageTestRange.gd`/several debug checks, so moving it to a new composed file would've relocated risk rather than reduced it. Instead it stayed on `Mech` as a pure extract-method split into `_reset_grid_state`/`_compute_mass_and_stat_modifiers`/`_simulate_energy_flow`/`_collect_weapon_mounts_and_tile_capabilities`/`_finalize_grid_state` - zero behavior change, verified across 24 regression checks (Cloak/Jammer/HealBeacon/TestRange/SimTimeline/GarageEditFix/TileConfig/all Jammer variants/MagnetRepel/Synergy/WaveScaling/TileRarity/Mortar/Blueprint/FleeThreshold/SearchEscalation/DirectorTells/RustExt/Rasterizer/MapConnectivity), all passing. The **damage/combat pipeline** (`apply_damage`/`apply_part_damage`/shield mitigation/disable rolls, ~366 lines) was audited last and found already healthy: it's 8 well-named, appropriately-sized private helpers (12-75 lines each), not one monolith. `apply_damage` has 24 external callers and `_show_floating_text`/`elemental_resistances`/`recent_damage_log` are accessed by name from SquadDirector/Main/BossBrain/Drone/HealBeaconSystem/PlayerController - moving any of it to a composed file would add an indirection hop with zero reduction in Mech's public surface. Left as-is; no extraction warranted. Tech-debt pass complete.
- **Consolidation-first mode:** per direction given 2026-07-16, no new features until this pass and a general stability sweep are done. The stability sweep itself landed 2026-07-27 (see Recently Shipped) - the 127 `scripts/debug/*Check.gd` regression harnesses were also scanned for actual bitrot (broken file references) as part of that pass and came back clean (0 found; all reference real, existing files) - the "127 flat files" complaint is a discoverability/organization concern, not dead code, so none were deleted. Still open: whether to reorganize them into topic subfolders for discoverability (not done - would need every matching `.tscn`'s `ext_resource` path updated too, deferred pending explicit go-ahead given the file-count risk).
- **Spatial Hashing:** Largely superseded by `EntityCache` + the saturation tiers; the remaining need (projectile broadphase) folds into queue item 4.

### Gameplay & Balance Improvements
- **Kinetic firing-mechanics enhancement (raised 2026-07-27, explicitly deferred - "later"):** the user wants Kinetic's cross-map sniping identity to feel more distinct/deliberate (charge-up? travel-time telegraph? extreme-range lock-on assist?), separate from its existing raw-range stat (already up to 5x base range, ~7000px full-Kinetic per `Projectile.gd`'s `KINETIC_RANGE_BONUS`). Raised specifically while discussing the `MechStatusBars` off-screen LOD gate - confirmed that gate is screen-visibility-based (`VisibleOnScreenNotifier2D`), not a distance radius, so it doesn't constrain this in any way. Zero design work done - flagged for a dedicated conversation, likely alongside Heat below (a cost/risk axis could be what makes a slower, more deliberate Kinetic playstyle pay off).
- **Heat as a system (raised 2026-07-26, explicitly deferred - "later"):** the user's framing - right now the weapons look cool but there's no real challenge beyond learning basic energy routing; a Heat mechanic is the leading candidate for adding actual friction/risk to firing, and ties directly into the "elemental meta is calcified" problem above (a cost axis that hits the fast/spammy elements harder could be what makes Vortex/Poison/Ice viable again). Zero design work done - purely a flagged topic for a future dedicated conversation, not scoped at all yet.
- **Boss Variety:** Stop scaling up Brawlers. Use role-specific bosses or genuine boss-only kits.
- **Synergy Effects:** Signature status marks per element are shipped; deepen the lingering effects for VORTEX and VAMPIRIC (bleed/immobilize interactions with corpse statues).
- **Elemental meta is calcified (raised 2026-07-27):** the user's read on current play - Lightning/Explosive/Kinetic/Pierce clear the map in seconds and dominate every build; Vortex and Poison are pariahs (slow/disruptive in ways that cost more than they're worth), Ice's projectile slowdown makes it hard to justify packing at all. No fix designed yet - needs a real balance pass, likely tied to the Heat system discussion below (a universal cost/risk axis might be what's needed to make the "slow" elements pay off rather than just nerfing the fast ones).
- **Anchor tile - base rarity is a real enabler, Mythic tier is the actual gap (confirmed via code + corrected via user testing, 2026-07-27):** initial read was that ANY-rarity Anchor's `is_vortex_immune` (`Mech.gd:1760-1764`) only zeroes out your own Vortex weapon's self-pull, and since that self-pull multiplier is just 0.05x (`Projectile.gd:1364-1379`) it looked like a near-no-op - that was wrong. `pull_strength = 600 * vortex_ratio * pull_mult * magnitude_pull_mult`, and `magnitude_pull_mult` scales up to 3x with power, so even at 0.05x self-pull that's up to ~90 units/sec of sustained drag while your own packet lingers, compounding across a burst - confirmed by the user: enough to drag the player across the whole map. So base-rarity Anchor is actually the thing that makes firing Vortex yourself controllable at all - a real prerequisite for Vortex being usable, not incidental. The genuinely open question is narrower than first thought: the 50% pull-reduction/25% damage-reduction against an enemy's Vortex only applies at Mythic (`vortex_pull_protection_ratio`/`vortex_damage_reduction`, added 2026-07-26) - whether that enemy-side defense should extend to lower rarities too is still a real balance call, just a smaller one than originally framed. Not changed yet - needs a decision, not a fix.
- **Player mech visual identity gap (found via code 2026-07-27, ties to the "factions" question):** the player's own mech is HARD-CODED to one fixed look regardless of build - `MechRenderer.gd:97`'s `_get_role_visual_profile("player" if is_hero else role)` always uses the fixed "player" profile (bright red, 1.05 scale, "hero" accent) for the human player specifically, completely ignoring `combat_role` even though every enemy role (sniper/brawler/scout/etc.) gets its own distinct palette+accent. Two more contributing factors: (1) a component's outer hex-grid SILHOUETTE is generated once when that component is first created and stays fixed as long as you keep it - swapping individual TILES into the same hex slots only changes the bulk/sleek stretch axis (via `_compute_visual_factors`, keyed on tile TYPE categories present) and conduit glow accents, not the base shape, so reusing the same torso/arm/leg reads as "no visual change" even across very different loadouts; (2) Corporate Sponsorship brand tiles are tagged with `brand_id` (`BrandTileFactory.gd`'s own comment calls out a planned "dark-blue/logo render override") but `MechRenderer.gd` has zero references to `brand_id` anywhere - that override was never actually built. Real foundation exists for a "factions = different assembly/render schemes" feature (role-based profile table already exists and already varies per-role for every non-player mech) - it's just never wired up for the player, and the brand-tile visual hook was stubbed out and abandoned. Not fixed yet - needs a design decision on how player visual identity should vary (by chosen role? by dominant brand affiliation? by build composition?) before implementing.
- **Near-peer difficulty watch:** evolution now earns premiums for player/ally damage and killing blows - watch whether it needs a counterweight (rubber-banding, bounty rewards for the player) as it sharpens.
- **HUD & UX:** damage-number noise (popup budget shipped), unify menu keys (shipped), HUD legibility (shipped). Extend Synergy Codex to cover biome interactions still open.
- **Scout torso shape:** a Scout-role torso should be leg-shaped - 3 hexes wide, variable length (matching the existing leg silhouette code) - rather than the current symmetric-disc torso shared by every role. Note: the recent torso-link-reachability fix deliberately removed the OLD scout/brawler torso thinning (it broke peripheral-link routing); this is a from-scratch replacement silhouette, not a revert, and must keep the 6-neighbor core hub + spoke-tip link placement guarantee that fix added.
- ~~War Room "recent deaths" panel~~ - **already shipped**, found already-built during 2026-07-27 audit: `WarRoomMenu.gd`'s Death Log tab (`_build_death_log`) shows every recorded death (wave/attacker/timestamp) reading `SaveManager.death_log` (rolling, capped at 20, save/load round-tripped). Not gated on garage-entry-from-death specifically (shows full history any time War Room is open) - arguably more useful than the originally-scoped version. Removed from backlog.
- ~~Test Range: HP override field~~ - **shipped 2026-07-27**: `GarageTestRange.gd` now has a "Dummy HP" text field next to Reset Dummy; blank falls back to the old default (1e12).
- **Elite Four rival tier (dialogue written, not wired up - captured/expanded 2026-07-22):** named rivals one tier above the 15 Regulars, open-ended despite the name (5 already: Hrothgar, Dan, Evan, Joe, Frank) - each carries a loose D&D-class flavor (Hrothgar = wizard, huge AoE mortars/fireballs, escalates through the fight; Dan = barbarian, starter gear + pure aggression, wins on fundamentals not power; Evan = rogue, deliberately under-powers shots to stay under a sensor-sweep threshold, first face of the dystopian-hints thread; Joe = uses parts in unexpected ways, gimmick genuinely undesigned yet, may get bespoke part geometry as his signature edge; Frank = bard/twist finale, the shop owner himself, adaptively counters whatever the player's been leaning on all game). Full dialogue (gimmick/intro/win/loss/round monologues) is in STORY_SCRIPT.md's new "Elite Four" section.
  - **Moddable roster (not yet designed):** the user wants this tier specifically to be an open, addable-to roster via modding - "elite enemies" as a mod-extensible category, not a fixed hardcoded 4/5. Naturally pairs with the already-planned Modding Phases 3-4 (Future Expansions, below) - would need Elite-tier rivals defined the same data-driven way `TileStatsRegistry`/`res://tiles/<TileType>/stats.json` already does for tiles (a per-rival JSON/resource: gimmick params, dialogue lines, drop table, spawn condition), rather than another hardcoded rival array, specifically so a mod can add a new one without touching engine code.
  - **Spawn/drop/difficulty-cliff mechanic (design only):** Elite-tier rivals only appear at very late, specific milestone waves (proposed: 1000, 1100, 1200, 1300, 1400...) and drop "absurdly good" unique components when beaten. Deliberate design tension: AFTER an Elite fight, normal wave scaling stops being enough to protect the player - regular enemies can crush a player who doesn't have (or hasn't built around) the Elite-drop parts, and the Director specifically evolves counters to whatever the player built with them even once they do. Needs real design work on exactly how "scaling goes out the window" is implemented (a difficulty curve change, not just bigger numbers) before this is buildable.
  - Still needs, regardless of the above: an unlock gate (after all 15 Regulars beaten at least once) and real gimmick implementations wired into whatever system drives the existing Regulars' mechanical behavior - dialogue alone doesn't make any of them playable.
- **More "transparent" hex tiles (captured 2026-07-22, design deepened 2026-07-27):** the tile roster is short on simple, low-commitment structural options - Directional Conduit is the only real "does basically nothing, just routes energy" tile, Amplifier is rare, and a player who doesn't want to commit a cell to Infusion or Catalyst's bigger effects has few other low-key choices. Candidate additions (all buildable with the existing tile/stats.json pattern, no new architecture needed): Structural Strut (zero energy logic, pure hex-bridging filler - the literal floor of commitment), Buffer Cell (holds one packet one tick then releases unchanged - timing control, no power), Passive Tap (copies a packet to a second direction at full magnitude, unlike Splitter's ratio-based split), Weak Mirror (stripped-down Reflector - redirect only, no damage/defense upside), Insulator (small flat overcharge-instability reduction, doesn't touch the intentional energy-loop mechanic itself).
  - **Circuit-logic angle (raised 2026-07-27, design-only):** rather than (or alongside) pure filler tiles, the user is weighing whether some of these should carry actual player-configurable LOGIC - e.g. gating/timing based on Sync state - which would make Resonators (currently a mid-tier amplify tile) much more central/foundational to mech design than they are now, since Sync would become a value worth deliberately engineering around rather than an amplify-and-forget stat. Open question: is the payoff (deeper build expression, "circuit design" as its own skill) worth the complexity cost against the "low-commitment filler" goal these tiles were originally meant to solve - the two goals may want different tiles rather than one shared solution. Needs a real design pass before any of this is buildable; not scoped yet.
- **Frank's Challenges - modifier ideas (design only, captured 2026-07-22):** two candidate scenario modifiers for the Tactical Puzzle Challenges feature (Execution Queue item 5) -
  1. **Flat damage reduction on enemies:** a straightforward stat gimmick, but a meaningful one - it specifically punishes loadouts built around many weak/spammed projectiles (each hit gets flattened toward zero) rather than a few heavy hits, forcing a shift toward big single-payload "megashot" builds to clear the floor. Good anti-synergy pressure against the opposite playstyle from Rudy's overclocking gimmick.
  2. **Per-projectile damage GATING ("The Eye"):** the inverse idea - an enemy that punishes HIGH power instead of rewarding it. In-fiction justified as a Neighborhood Watch sensor sweep rather than a raw number: on patrol nights, full-power weapon signatures interfere with its sensors, so it takes bonus damage (or is only vulnerable at all) from LOW-power hits and shrugs off/reflects anything over a threshold - forces restrained, low-magnitude builds instead of alpha-strike ones. First concrete hook for the dystopian-hints worldbuilding thread (see STORY_SCRIPT.md) - the challenge's flavor text is where "unauthorized electronics/thermal signature sweeps" gets introduced to the player.

### Future Expansions
- **Synchronous Online Multiplayer (GodotSteam):** P2P lobby punch-through using GodotSteam (no dedicated servers required).
  - *Network Model:* State-sync/Server-Authoritative with Client Prediction (since the combat/mat phase is explicitly non-deterministic by design; only the Garage hex-routing is deterministic).
  - *Game Modes:* 1v1 duels, 2v2 brawls, 1+1 / 2+2 Co-op wave survival.
  - *Asymmetric PvP (2v1):* A point-buy system for components before the match where the solo player receives twice the point budget as the duo players.
- **Expedition Map:** Slay-the-Spire style branching Tournament Bracket/Shop Campaign.
- **Pilot Skill Tree:** Earn XP for permanent QoL/utility passives across runs (QoL, not power, per ruling).
- **Deployables & Superweapons:** Proximity mines, turrets, massive multi-hex superweapons (e.g. AoE shield domes).
- **Modding (Phases 3-4):** Custom bot types and custom tile scripts extending `HexTile` (staged by queue item 1).
- **Cutscene content:** the framework is shipped and data-driven (`config/cutscenes/`) - actual story beats and real sprite PNGs are authoring work, gated on STORY_SCRIPT.md.

---

## 4. Recently Shipped (orientation only - cleared from tracking)
- **Full-codebase stability sweep (2026-07-27):** 5-way parallel audit across core/entities, AI/combat/hazards, all 32 tiles, UI/visuals/debug, and the Rust extension, followed by fixes for every confirmed bug found: `MissileRackTile.gd` missing its `class_name`/`extends` header (wouldn't compile), Lance Beam's `check_face_gate()` never being called before `clear_pending()` wiped its state (the weapon could never fire in real gameplay), a double death-report panel on player death (`Main.gd`), a squad-merge signal-leak double-counting damage telemetry into two squads at once (`SquadDirector._merge_squads`), missing `reset_simulation_state()` overrides on Lance Mount/Orbiting Array/Missile Rack/Component Link/Magnet/Jammer Module/Drone Bay (Timeline Scrubber replay determinism), a missing `tiles/OrbitingArrayTile/stats.json`, Settings menu stacking duplicate instances on repeat clicks (MainMenu/PauseMenu), JumpjetResidue picking damage element by presence instead of magnitude, and a family of Rust FFI panics (`.to()` instead of `try_to()` on Variant/Dictionary data from GDScript, a Splitter weights/faces length mismatch, a Lance cell-index silently misattributing to the anchor cell) across `hexgrid_sim.rs`/`part_rasterizer.rs`/`projectile_flight.rs`/`projectile_broadphase.rs` - all rebuilt and verified compiling clean (debug + release). Also corrected two stale doc entries this sweep surfaced: Corporate Sponsorships and the War Room Death Log were both already fully shipped despite still being listed as open backlog. Dead `GarageUI.gd` prototype (never instantiated) deleted. Full findings detail lives in that session's transcript, not restated here - this line exists so a future assessment doesn't need to re-run the same audit.
- Easy-wins batch: starter inventory rarity curve (no more free Legendaries), Ambusher decloak reveal burst + targetable Heal Beacons (disable-priority), HUD text outlines + unified menu keys (WarRoomMenu now uses real InputMap actions like everything else), Champion Card sprite portrait (real in-engine MechRenderer render composited above the existing hex-schematic blueprint via a throwaway SubViewport rig)
- Data-driven tiles: all 24 tile types + `TileStatsRegistry` autoload, `res://tiles/<TileType>/stats.json` per type, zero behavior change (verified)
- Simulation Timeline Scrubber + Packet Inspector: deterministic re-run-to-step-N replay (no state buffering - Resonator/Splitter/Catalyst mutable state resets via HexTile.reset_simulation_state before each replay), drag-to-scrub HSlider synced to live auto-play, click-a-tile packet inspector showing current output + last-5-packet history per direction
- Garage Test Range: live-fire any armed mount's real combat packet at a target dummy in a private physics world (SubViewport + own World2D) - real projectiles, patterns, damage numbers, shots/avg readout
- Tile config batch (`1300fb6`): Resonator per-path Sync Dropoff, Mythic Splitter output ratios, Catalyst gated injection (magnitude + cadence gates), Accumulator auto-dump thresholds - all in the click-a-tile config popup, all save-persistent
- Wild-bot flee thresholds: skittish roles break off and go wild (squad + wave accounting stay honest, wounds regen while loitering, director recruits them back)
- Saturation perf tiers: volley consolidation, projectile visual LOD, damage-popup budget (`d65ab79`)
- Drone sortie leash fix (1000/2000px), near-peer AI fitness premiums, Mythic boss torso hex-budget fix (`d65ab79`)
- Pixel-art cutscene framework + sample Frank beat (`6bc1b63`)
- Power-scaled jammer fields + minimap jammer secrecy swirl (`3f3df78`)
- Unlimited named save slots for builds and parts (`fa26417`)
- Earlier: Scrap Shop, captured loadouts, Lance Mount, recon/Chinook drones, lightning blink identity, mortars with true direct-fire payloads, strategic zoom, demo builds, module keybinds, obstacle corridor carving.
