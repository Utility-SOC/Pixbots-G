# PixBots-G: Master Status Document (Active Work & Backlog)

This document tracks the active implementation targets, backlog, and design expansions for PixBots-G. (All shipped and completed features have been cleared from this tracker - see the short "Recently Shipped" section at the bottom only for orientation context.)

---

## 1. Lore & Vision Context
**The Core Fantasy:** You are playing *PixBots*, a wildly popular tabletop miniatures game at Frank's local hobby shop. The scale is 20mm miniatures fighting on a 4x8ft tabletop mat (1mm = 5px, 1 inch = 127px).
**The Goal:** Survive as many waves as possible against Frank's proprietary AI Director, which evolves and breeds new rival profiles to counter your strategies.

**Story (Canon):** The shop runs a standing competition: defeat the AI and earn merchandise. The Black Market is the shop's glass case; the War Room is your opponent studying your plays.

**Open worldbuilding thread (not yet authored, direction only):** hints that the world outside the shop is a dystopian collapse Frank never comments on directly - see STORY_SCRIPT.md's "Worldbuilding thread: dystopian hints" section for seed ideas (Neighborhood Watch sweeps for unauthorized electronics/thermal signatures/unregistered materials) and the "The Eye" challenge concept below that gives it a first concrete gameplay hook.

**Standing design rulings (locked):**
- Purely procedural 2D visuals; sprite PNGs are optional *overrides*, never requirements.
- Every Champion Card PNG carries the full buildout (ghost + blueprint duality).
- Explosions are payload-only (no flight identity). Skill trees are QoL, not power.
- Corporate Sponsorships bias loot via **weights, never hard filters** - no build path can be bricked.
- Pixelbots 2 (godot-3d) shares gameplay rules via Node-free RefCounted/static classes, targeting an engine-agnostic `pixbots_core` Rust crate long-term.

---

## 1a. Pixelbots 2: The 3D Vision (captured 2026-07-20, not active work)

This is the actual long-term destination the `pixbots_core` split above is aimed at. Recorded here as durable design intent, not a task queue - **PB1 stays the priority**, and PB1 only picks up changes from this vision when they're cheap/natural to do anyway (e.g. work already being done for other reasons that happens to also serve PB2). Nothing below should be read as "go build this now."

**The core idea:** the hex grid isn't 2D forever - it's the 2D unfolding of a **3D cube grid**. Each hex a player places today is a face-projection of a cube; when the game goes 3D, that same build data describes a genuine 3D voxel structure. Garage UX changes accordingly: selecting a limb zooms the camera into a 3D model of that limb, and the player sees/edits columns and rows of cubes directly on the model - build logic and rules stay hex-based (so PB1's balance, routing, and solver code ports largely as-is), but the *view* is a real 3D shape, not a flat schematic.

**Why this is exciting, in the user's own words:** the hex-grid shape rules already produce "weird long limbed" torsos and asymmetric builds in 2D - wrapped up into 3D cube volumes, those same shapes read as genuinely strange, cool silhouettes rather than just an odd flat outline. The build-in-hex/view-in-3D duality is the whole hook: familiar hex-grid engineering, alien-looking robots.

**What this implies architecturally (informs, doesn't mandate, PB1 work):**
- The hex-grid simulation (packet routing, tile process_energy, capture/store semantics - i.e. everything `hexgrid_sim.rs`/`RustGridSim.gd` now cover) is exactly the "gameplay rules" layer meant to become the engine-agnostic `pixbots_core` crate. Every tile ported to Rust in PB1 is (long-term) a tile PB2 doesn't have to re-implement - this is the concrete throughline from "port tiles to Rust for PB1 performance" to "build the PB2 foundation." They're the same work, not competing priorities.
- Shape-generation code (`ComponentEquipment.generate_shape`/`generate_procedural_shape`) is the other natural PB2 seed - it already reasons about hex geometry as pure data (no Godot Node coupling in the algorithm itself), which is what a cube-projection layer would need to consume.
- The engine question is open: Godot supports 3D natively (no engine swap forced), but a 3D-heavy sequel is also the natural point to revisit whether Godot remains the right fit vs. something with more mature 3D tooling - not a decision to make now, just flagged as a real fork in the road when PB2 actually starts.

**Explicit non-goal for PB1:** PB1 does not need a 3D renderer, a cube-projection layer, or any camera/zoom-into-limb UX. Its job is only to keep the simulation/shape-generation layers clean and engine-agnostic where that's low-cost, so the eventual extraction is easier.

**Axial-to-cube axis mapping (raised 2026-07-27, purely conceptual, not to-do yet):** the user's own framing of "rolling up" a hex line into a line of cubes - a single straight line of hexes stays a straight line of cubes, but the existing "weird long-limbed dog-leg" shapes (already produced by the current hex shape-generation budget/spoke rules) would read as genuinely strange 3D silhouettes once every bend becomes a real turn in 3D space rather than a flat direction change. Tentative axis idea floated: hex E/W -> world X, NE/SW -> world Y, SE/NW -> world Z (an axial-to-cube-diagonal mapping, since a 2D hex grid has 3 natural axes/6 directions, matching a cube's 3 axes/6 faces one-to-one) - plausible on its own terms but genuinely untested; needs real validation once PB2 work actually starts, not now.

---

## 2. Execution Queue (in order)

Garage QoL first (felt immediately), then small gameplay wins, then progression systems, then the big engine lift, then the feature batch.

1. **Data-driven tiles** - shipped. Every one of the 24 tile scripts now reads its balance numbers (weight, charge/damage multipliers, per-rarity power/duration/interval curves) from `res://tiles/<TileType>/stats.json` via the new `TileStatsRegistry` autoload, falling back to the original hardcoded value if a file/key is missing - purely additive, verified byte-identical to prior behavior via `DataDrivenTilesCheck.gd` across all 24 types and all 5 rarities. Optional sprite PNG override per tile folder is a natural next step (not yet wired) for the full custom-tile-mod staging; procedural visuals remain the default per the locked ruling either way.
2. **Late-game progression** (in order):
   - ~~*Overclocking (prestige)*~~ - **shipped 2026-08-03.** Repeatable, per-component, gated on `rarity >= Mythic` (the closest existing, already-enforced "max component level" ceiling - no unified component-level stat existed before this). Also reworked the existing shipped Mod Chip system (`player_modifier_chips`/`TileActionMenu.infuse_chip()`) into something reversible and capacity-gated - the original one-way, non-slotted mechanic didn't match this feature's "un-equip chips back to inventory" requirement, so `infuse_chip()` became `equip_chip()` + a new `unequip_chip()`, backed by a new `ComponentEquipment.equipped_chips: Array[Dictionary]` (individually tracked, reversible) instead of the old opaque merged-float `stat_modifiers`. `stat_modifiers` itself stays the same field name/shape (zero changes needed at any of its ~9 existing read call sites - `Mech._compute_mass_and_stat_modifiers`, `GarageInventoryPanel` tooltips, `SquadDirector`, `SaveManager`) but becomes a derived cache, rebuilt by `_recompute_stat_modifiers()` from `infusion_stat_modifiers` (uncapped, matches the original real behavior) + `legacy_stat_modifiers` (see save migration below) + `equipped_chips` (capped at `CHIP_STAT_CAP = 1.5`, but - a real design nuance caught during implementation - the cap only ever applies to a stat a chip actually touches, so a stat raised purely by uncapped infusion rolls is never retroactively capped by this rework). Chip capacity (`get_chip_capacity()`) is derived, never stored raw - rarity-scaled base (`[2,3,4,5,6]` Common..Mythic), drops by 3 per overclock cycle, recoverable via a new paid `recalibrate_chip_capacity()` action but never past the pre-overclock baseline. The reward choice (permanent mass reduction vs. +1 hex) is a real player-facing popup, built by reusing `GarageTileConfigPopup`'s already-proven click-outside-to-dismiss wiring (instantiated as a helper from `TileActionMenu`, not refactored to static - avoided touching its 11 existing call sites) rather than duplicating that logic. Mass reduction (`ComponentEquipment.mass_reduction`, lives on the component so it follows the part across mech swaps) plugs into `Mech._compute_mass_and_stat_modifiers()` with a `MIN_TOTAL_MASS = 10.0` floor guarding `_get_mass_speed_mult()` against zero/negative mass; the hex reward reuses the existing `pending_expansion_hexes`/click-to-place pipeline from rarity-tier upgrades entirely as-is. **Save format bumped to v5**: pre-rework saves carry an opaque `stat_modifiers` blob with no per-chip provenance (the old `infuse_chip()` did an irreversible running sum) - reconstructing individual chips from it is mathematically underdetermined, so the migration preserves the whole total as a permanent `legacy_stat_modifiers` bonus instead (no power loss, no invented chips), `has()`-guarded so pre-v5 saves still load correctly. Verified via new `OverclockingCheck.gd` (43 assertions: capacity formula, equip/unequip round-trip, capacity enforcement, cap semantics, overclock gating/effect, both reward paths, repeatability, recalibration, the mass floor, save/load round-trip, legacy migration) plus the existing `DebugChipGrantCheck.gd` (updated for the rename), `GarageTabPreservationCheck.gd`, `TestRangeCheck`, `TileConfigCheck`, `NamedLoadoutCheck`, `AccumMicrocoreSerializationCheck` all re-run clean. Full design plan at `C:\Users\Utility\.claude\plans\effervescent-drifting-kazoo.md`.
   - ~~*Chip Splicing*~~ - **shipped 2026-08-04.** Two-tier chip-merge system, replacing the single-trait chip shape everywhere with `{"traits": Array[{"stat","value"}]}` (plain = 1 trait, "Corrupted" = 2+, unbounded). **Tier 1** (first splice, two plain chips with DIFFERENT stats, no match required): both source stats boosted `SPLICE_BOOST_MULT = 1.25x` beyond their original bonus, plus one random negative trait (magnitude `0.05-0.20`, drawn from `ComponentEquipment.CHIP_STAT_POOL` minus the two used stats) - 3-trait result, both inputs consumed. Two plain chips sharing a stat deliberately do NOT qualify for Tier 1 - they fall through to Tier 2 instead. **Tier 2** (re-splicing a Corrupted chip with anything): requires at least one shared stat; the union of stat names nets PURELY ALGEBRAICALLY per stat (`(a-1.0)+(b-1.0)`, no boost - the one case that never stacks a multiplier), unmatched stats carry over unchanged, trait count is unbounded (no cap/eviction), and a splice that cancels a stat to ~0 drops that trait silently rather than leaving a dead zero entry; a splice that cancels to ZERO total traits is rejected outright (`{}`, both inputs untouched - losing two real chips to a math coincidence felt like the wrong default). New stateless `ChipSplicer.gd` (`class_name ChipSplicer extends RefCounted`, `static func`s only, same split-out-logic convention as `BossEvolution.gd`) hosts `classify_splice()`/`splice_tier1()`/`splice_tier2()`/`splice_chips()`. New Splice UI in `TileActionMenu.gd` (`open_splice_popup()`, reuses the same lazily-instantiated `GarageTileConfigPopup` helper Overclocking's reward popup already uses) lets the player pick up to 2 pool chips and commit; a new `_describe_chip()` formatter (`"[Corrupted] fire_mult +15%, ice_mult +22%"`) replaced every direct `chip["stat"]`/`chip["value"]` read across the UI. **Safety fix caught during implementation, not in the original design**: `CHIP_STAT_CAP` only ever bounded the upper side; Tier 2's real negative-trait stacking could otherwise drive a stat like `dmg_mult` below 0.0, and `Projectile.gd`'s `damage *= stat_modifiers["dmg_mult"]` would then produce NEGATIVE damage (a hit that heals instead of harms) - closed with a new `CHIP_STAT_FLOOR = 0.1`, mirroring the cap's exact scope (only ever clamps stats a chip actually touches). **Enemy chips**: only Elite Four rivals and bosses get 1-3 real single-trait chips equipped onto their actual loadout at spawn (`Main._equip_enemy_chips()`, bonus `0.15-0.35`, called from `_spawn_boss`/`_spawn_rival` - NOT `SquadDirector.gd`'s shared rank-and-file builder, confirmed by direct code read after the planning agent's first draft got the location wrong) - real, worthwhile power, not a token drop. **Death-drop**: on death, only PLAIN equipped chips roll a chance-based drop (`LootManager.CHIP_DROP_CHANCE = 0.20`, consistent with existing drop-rate conventions) via a new `LootPickup.chip_data` pickup kind (amber-diamond visual, same established `tile_data`/`equipment_data` three-way branch pattern); Corrupted (spliced) chips are simply never eligible - "an enemy can only drop prespliced/unspliced chips," per the user's design, so mech destruction effectively destroys any Corrupted chip outright. **Save format bumped to v6**: new `SaveManager._migrate_chip_shape()` losslessly promotes any pre-v6 flat `{"stat","value"}` chip to single-trait `{"traits":[...]}` (every pre-splicing chip was always single-trait, so this can't lose data), applied in both `_deserialize_component()`'s `equipped_chips` restore and `load_game()`'s `modifier_chips` restore. Two real bugs were introduced and caught during implementation, both via script-error output on test runs, not via the assertions themselves: a stray displaced line that broke `TileActionMenu.gd`'s compile entirely (`Invalid call. Nonexistent function 'new'`), and a leftover raw `chip["stat"]` read in `unequip_chip()`'s feedback string that `OverclockingCheck.gd`'s unequip round-trip surfaced. Verified via new `ChipSplicingCheck.gd` (33 assertions: Tier 1 boost/negative-roll correctness, Tier 2 zero-overlap rejection + sign-flip + full-cancellation, the user's own fire/ice/vortex/lightning worked example reproduced numerically end-to-end, same-stat-plain-chips-fall-through-to-Tier-2, `CHIP_STAT_FLOOR` enforcement, save/load round-trip, legacy migration, `Main._equip_enemy_chips()` called directly - not reimplemented - against a real `Mech`, and the plain-only death-drop rule) plus `OverclockingCheck`/`DebugChipGrantCheck` (updated for the new chip shape) and `TestRangeCheck`/`TileConfigCheck`/`NamedLoadoutCheck` all re-run clean. Full design plan at `C:\Users\Utility\.claude\plans\effervescent-drifting-kazoo.md`.
   - *Nemesis Bounties:* the Director breeds a boss from the player's own genetics (BossEvolution) to counter their build; drops unique inverted-synergy Nemesis Parts. Not started.
3. ~~Rust projectile physics port (Phase 3)~~ - **shipped 2026-07-27.** Turned out a prior antigravity session (2026-07-24, plan at `C:\Users\Utility\.claude\plans\effervescent-exploring-pine.md`) had already built Days 1-2 (Rust `query_hits` swept-circle-vs-segment test, the `ProjectileBroadphase.gd` autoload batching registration/reports/dispatch, `PartHitbox`/obstacle/husk group+radius registration, and a parity check) but never wired it into `Projectile.gd` itself - this session found that plan, verified Days 1-2 with the existing parity check (passed clean), and executed Day 3: `Projectile.gd` now `extends Node2D` (was `Area2D`), no more per-projectile `CollisionShape2D`/physics-server registration - every projectile reports its swept movement segment to `ProjectileBroadphase` each tick, which runs ONE batched Rust call for all live projectiles against every live `PartHitbox`/obstacle, then dispatches hits into the unchanged `_handle_hit()`. Fixed the 3 call sites that touched the old Area2D API (`MortarShell.gd`, `HitDecayCheck.gd`; `MagnetSystem.gd` needed no change). Verified: parity check (Rust == GDScript fallback, exact pair match), `HitDecayCheck`, `ProjectileMaterialSharingCheck` all pass; full headless smoke test clean, no stray processes. Real playtest evidence (2026-07-27 stress video, wave 12, 495 live shots, 34 enemies): collision pairs stayed at 9 - broadphase is confirmed NOT the bottleneck anymore, exactly as intended. **Not done manually**: the plan's own Day 4 mechanic checklist (pierce multi-hit, lightning chain re-targeting, poison mine detonation, Mortar direct-fire + lightning re-arm, Magnet repel reflection, no new tunneling) - recommend a real playtest pass to confirm before calling Phase 3 fully closed out.
4. **Rust Phase 4 (captured 2026-07-27, scoped from real evidence, not started):** the same stress-test video that confirmed Phase 3 also surfaced the NEXT bottleneck - `shoot` (896ms/sec) dominated `projectile_physics` (231ms/sec) in FpsCounter's real in-session overlay, the opposite of what was assumed going in. Two follow-up diagnostics (both built and run this session) redirected the target twice:
   - **`ProjectileConstructCostDiagnostic.gd`** (new, reads `Mech._perf_shoot_usec`/`HexTile._perf_projectile_construct_usec` DIRECTLY - real accumulated counters from real `_shoot()`/`_fire_combined_projectile()` calls, no wall-clock diffing, so no warmup-noise risk): of 600 real fired shots, projectile construction (`Projectile._ready()`/`_build_visuals()`) was only ~33% of shoot cost - the merge/pattern/consolidation packet math in `HexTile._fire_combined_projectile` was the bigger share at ~67%. **Shipped two passes** (2026-07-27), both verified via `TestRangeCheck`/`TileConfigCheck`/`DataDrivenTilesCheck` PASS and re-measured via `ProjectileConstructCostDiagnostic.gd`'s real accumulated counters (no wall-clock noise risk): (1) stripped ~7 always-true `"field" in proj/mech/packet` runtime existence checks plus `.get()`/`.set()` dynamic dispatch in `_fire_combined_projectile` (`proj`/`mech`/`packet` provably always have every field touched there - `_ProjectileClass` is a `const` preload, never polymorphic) for direct member access - 38.82ms -> 37.13ms across 600 real `_shoot()` calls (~4%); (2) added `EnergyPacket.add_synergies_batch()` (syncs magnitude/synergy proportions ONCE per merge instead of once per synergy key - `_sync_synergies_to_magnitude()` is O(n) in synergy count, so the old per-key `add_synergy()` loop was O(n*k) of redundant resyncing) and used it at `_fire_combined_projectile`'s two consolidation-buffer merge loops AND the same copy-pasted pattern found in `ResonatorTile.gd`/`SplitterTile.gd`/`brands/PrimeCircuitTile.gd`'s remnant-magnitude folding (real grid-simulation hot paths too, not just shoot) - 37.13ms -> 36.82ms (~1%). **Honest verdict**: real, permanent, safe wins, but only ~5% combined (~0.061ms/shot) - this hot path just isn't that expensive per-call. The video's 1-2fps collapse was suspected to be aggregate call VOLUME and/or rendering overdraw ("1727 draws") - **the rendering half of that guess was wrong, see the 2026-08-03 "Rendering-overdraw investigation" entry below: draw-call volume tracks projectile count but isn't the actual cost driver, per-projectile `_physics_process` cost at scale is.** Further GDScript micro-optimization on this specific region (shoot-time packet math) has hit diminishing returns.
   - **`CharacterBodyExistenceCostDiagnostic.gd`** (new, warmup-corrected re-test of a prior session's claim that bare `CharacterBody2D`+`CollisionShape2D` registration costs ~350ms/tick at 60 mechs just by existing): the corrected test reproduced the same large cost even after a 20-frame warmup, BUT tracing it further found the real cause - `Mech._ready()` unconditionally builds a full 6-slot starter loadout (`ComponentEquipment.create_starter_*` x6) AND calls `MechRenderer._rebuild_visuals()` (including the `Geometry2D.merge_polygons` silhouette computation already flagged elsewhere as a real spawn-stutter cost) for EVERY mech instance, regardless of whether the caller intends it to stay "bare" - so neither this test nor the original session's diagnostic ever actually isolated bare physics-body cost; both were unknowingly measuring full starter-body construction + first-time silhouette computation instead. **Corrected finding, not a new mystery**: this converges with the already-known, already-partially-mitigated (`_component_polygon_cache`) MechRenderer polygon-union cost - a genuinely isolated bare-CharacterBody2D test (skip `equip_component` entirely) is still open if the question matters enough to pursue further, but is lower priority now that the real cost has a known owner.
   - **Rust-portable candidates identified but not yet built**, in priority order: (1) ~~packet/pattern math in `_fire_combined_projectile`~~ **tried 2026-08-03, reverted - see below**, (2) ~~homing-target search + Vortex pull-scan batching in `Projectile.gd`~~ **tried 2026-08-03, mixed result - see below**, (3) ~~sight/search raycasts~~ **shipped 2026-08-03 as part of the AI-tactics Rust-cutover plan (Phase 2) - see that section**, (4) ~~`MechRenderer`'s `Geometry2D.merge_polygons` silhouette union~~ **fixed 2026-08-03 - NOT a Rust port, see below: the real problem was the algorithm, not the language.**
   - **MechRenderer polygon-union cache-miss cost: fixed 2026-08-03 - algorithmic fix, no Rust needed.** Measured first (`ComponentPolygonMergePerfCheck.gd`, blob vs. "line" hex-cluster shapes at realistic sizes up to 45 hexes - "long limbed" torsos are a real, common shape family per the design notes, not an edge case): a compact blob scaled gently (1.3ms at 45 hexes), but a line shape blew up to **12.3ms at 45 hexes** - a real cache-miss stutter on every fresh boss/rival procedural roll (which never benefits from the existing per-shape cache). Root cause: the old algorithm blindly tried `Geometry2D.merge_polygons` against every OTHER remaining polygon each pass, repeating until nothing merges - O(n²) pairwise attempts per pass, and for a line shape (each hex only touches 1-2 neighbors) O(n) passes before the union stabilizes. The fix stayed in GDScript, no external geometry crate and no new Rust module: hex adjacency is already known exactly from `(q,r)` coordinates via `HexCoord.get_directions()`, so `MechRenderer._get_component_polygon` now does a BFS spanning walk over that KNOWN adjacency graph, merging each newly-visited hex's polygon into its connected component's running union as it walks - exactly `n-1` merge calls total for a connected shape (real torso generation always grows from a single connected frontier), down from the old O(n²-n³). The underlying geometry math is untouched (same `Geometry2D.merge_polygons` calls between genuinely-adjacent hexes) - only which pairs get tried and in what order changed. Verified via a first-principles ground truth rather than comparing against the old implementation's specific output: for hexagons tiling a grid with no gaps or overlaps, a shape's total area must equal EXACTLY `hex_count * single_hex_area` regardless of merge order - new `ComponentPolygonMergeCorrectnessCheck.gd` confirms this exactly (shoelace-formula area, 0.001% tolerance) across blob/line/zigzag topologies from 1 to 45 hexes, 24/24 pass. `MechRendererPolygonCacheCheck.gd` (cache correctness/safety) re-run clean, unchanged. Measured: **line shape at 45 hexes dropped from 12.3ms to 1.6ms - a 7.6x speedup** and now scales roughly linearly instead of blowing up; blob shape also improved (~1.4x, fewer redundant re-scan passes even in the already-cheap case).
   - **Homing-target search + Vortex pull-scan batching (task #33): honest mixed result, 2026-08-03 - homing shipped, vortex reverted.** Baseline measured first (`ProjectileTargetingPerfCheck.gd`, 100 projectiles/60 targets, matching the "100+ live projectiles" scenario `Projectile.gd`'s own pre-existing comment already flagged): real, meaningful aggregate cost. Built `rust_ext/src/proximity_query.rs` (`ProximityQueryRs`) - a general grid-bucket radius-query primitive (variable per-query radius, unlike `separation.rs`'s fixed radius) reused by both `_find_homing_target` and `_pull_nearby_items`'s replacements via a new autoload `ProjectileTargetingBatcher.gd` (mirrors `ProjectileBroadphase.gd`'s register-nothing/report-per-tick/resolve-once pattern). First same-process interleaved A/B (`ProjectileTargetingABCheck.gd`, the trustworthy methodology - a naive cross-process before/after comparison was checked first and, consistent with this session's own established lesson, gave a misleadingly different answer) found the initial `batch_radius_query` design **103% slower for homing, 44% slower for vortex** - both returned a full sorted hits-array per query, real unnecessary FFI/marshalling overhead when homing only ever consumes ONE result. Added `batch_find_best` (returns just the single closest/furthest match, no hits array) specifically for homing and re-measured: **homing now a real 23.9% win, shipped** (`_request_homing_target` batches through `ProximityQueryRs.batch_find_best`; `_find_homing_target` no longer called from the hot path, kept only as the A/B check's reference implementation). Vortex genuinely cannot use the same no-hits-array trick - it must apply a pull to EVERY candidate in range, not just one - and stayed measured at **46.2% slower**, matching the `packet_tax.rs`/Phase 4 precedent exactly; reverted `Projectile._pull_nearby_items` back to the real per-projectile physics query, left `ProjectileTargetingBatcher.request_vortex_pull`/`_resolve_vortex` in the tree unused. **Bonus find**: while testing, discovered a real, pre-existing production bug unrelated to this task - `LootPickup.pull_towards(target_pos, delta_mod)` only accepted 2 arguments, but every call site in the codebase (including the original, unmodified `_pull_nearby_items` and a detonation-burst path) called it with 3, meaning any Vortex-heavy shot that ever got near dropped loot in real gameplay threw a script error and silently skipped pulling it. Fixed by giving `pull_towards` an accepted-and-ignored optional 3rd `strength` parameter, matching the universal calling convention.
   - **Simulation LOD for distant/disengaged enemy mechs (task #34): weapon-charge throttle shipped 2026-08-03, rest deliberately deferred.** `Mech._physics_process`'s far-branch LOD (mosey movement, throttled boss AI-tactics) already existed, but the PREAMBLE above it (`update_status_effects`, `_tick_weapon_charges`, cloak/jammer/healer/shield-pulse ability ticks) ran unconditionally for every mech every tick regardless of near/far status - an earlier `MechPhysicsCostDiagnostic.gd` run had flagged this exact gap ("the always-run preamble... needs its own bisection round") but never followed up. Measured first (`MechPreamblePerfCheck.gd`, 60 real equipped mechs parked far from target): **10.86us/mech-tick combined** - `_tick_weapon_charges` was the single biggest piece at 5.09us (47%), `update_status_effects` 1.86us, ability-system ticks 3.91us. Fixed the biggest, lowest-risk piece: `_tick_weapon_charges` for non-player, non-boss FAR mechs now throttles to the SAME 4Hz cadence already established for far bosses' AI-tactics (`_lod_ai_timer`) instead of running every tick - far mechs already skip `_shoot()` entirely in the mosey branch, so keeping their weapons charged at full 60Hz precision was pure waste; accumulates real elapsed time between throttled ticks (same pattern `Projectile._pull_nearby_items` already used) so total charge over time still matches, and flushes any pending elapsed time immediately on a far->near transition so closing to engagement range never silently loses charge progress. Verified via new `WeaponChargeLodCheck.gd` (a throttled far mech's charge tracks an unthrottled near mech's within one throttle-interval's worth of real time, in either direction - two earlier draft versions of this check used tolerances that were too tight/directional and produced false failures on ordinary mid-cycle sampling noise before landing on the right bound; transition-flush confirmed monotonically non-decreasing). Measured: `_tick_weapon_charges` dropped **5.09us -> 1.16us/mech-tick (~4.4x)**, combined preamble **10.86us -> 7.10us/mech-tick (~35% reduction)**. `update_status_effects` and the ability-system ticks (~5.9us/mech-tick combined) stay unthrottled for now - deliberately deferred, not forgotten: both carry real behavior-sensitivity risk (status-effect DoTs/buffs, heal-beacon pulses, jammer auras) that the weapon-charge case didn't have (a far mech never shoots anyway, so a charge-readiness delay is a much softer, already-precedented tradeoff than pausing a burning mech's damage-over-time or a healer's pulse timing would be) - a real candidate for a future round, not ruled out.
   - **Simulation LOD, remainder shipped 2026-08-03 - `update_status_effects` + 4 ability-system ticks now throttled too.** Two Explore passes confirmed every remaining preamble piece was safe to throttle via the SAME elapsed-accumulator pattern (not a bare skip): `StatusEffectRunner.gd`'s DoT damage is delta-scaled (`apply_damage(5.0 * delta)` etc.), so accumulating real elapsed time and applying it in fewer/larger chunks preserves exact totals - only expiry-timing precision gets coarser (up to ~0.25s), imperceptible off-screen. Real hazard found and handled: `current_move_speed` is recomputed *inside* `update_status_effects` and feeds the mosey-movement branch, so the whole function is throttled as one atomic unit (matching how `_tick_weapon_charges` is already atomic) rather than split. `cloak_system.tick`/`_update_jammer_module`/`_update_healer`/`_update_shield_pulse` all already early-exit fast when the mech doesn't have the relevant part equipped, but the early-exit itself still cost something 4 separate times every tick - throttling helps regardless of whether the ability is equipped. `_is_far_for_lod` hoisted above `update_status_effects`'s call site so it could be shared; each of the 5 systems got its own independent timer/elapsed field pair (no shared helper - matches the codebase's existing preference for explicit per-site state over abstracting a handful of call sites, and avoids `Callable`-construction overhead on a hot path), same 0.25s/4Hz cadence as the existing precedent. Verified: existing `CloakSystemCheck`/`HealBeaconSystemCheck`/`JammerModuleSystemCheck` re-run clean (near/engaged mechs never throttle, so their behavior is untouched); new `StatusEffectLodCheck.gd` confirms a far mech's total burn-DoT damage over 3 real seconds matches a near mech's exactly (14.750 vs 14.750, both ~15.0 expected at 5 dmg/sec) within one throttle-interval's tolerance. Measured: combined preamble **10.86us -> 1.17us/mech-tick, a ~9.3x reduction** (`update_status_effects` 1.86->0.22us, `_tick_weapon_charges` further down to 0.42us in this all-far scenario, ability ticks 3.91->0.52us). The always-run preamble for distant/disengaged mechs is now fully LOD-gated.
   - **Object pooling for Mech/Projectile (task #35): scoped, 2026-08-03 - not implemented, recommend against Mech pooling, Projectile pooling inconclusive.** Per this task's own framing ("scope, don't build yet"), measured the real spawn+despawn CYCLE cost (`ObjectPoolingScopingCheck.gd`, distinct from the construction-CONTENT cost already measured/optimized earlier this session) before committing any implementation effort:
     - **Mech: ~9.09ms spawn + ~0.93ms free = ~10.0ms/cycle.** Confirms and extends this session's earlier `CharacterBodyExistenceCostDiagnostic`/`MechRendererPolygonCacheCheck` findings - the overwhelming majority of that cost is loadout-specific construction (starter-loadout equip, grid recalc, render pipeline) that a pool categorically cannot avoid, since every real enemy spawn needs a DIFFERENT loadout matching its own role/rarity/procedural rolls, not a reusable one. A pool would only ever save the underlying bare Node/CollisionShape allocation, a small fraction of the ~10ms total - not worth the real implementation risk (correctly resetting 20+ stateful systems - cloak, jammer, healer, shield-pulse, weapon mounts, synergy caches, squad membership - is exactly the kind of surface where a single missed reset silently leaks state between reuses into a real, hard-to-diagnose gameplay bug). **Recommend against pursuing Mech pooling.**
     - **Projectile: ~39.5us spawn + ~59.8us free = ~99.3us/cycle.** Real at high volume (up to "100+ live projectiles"/"441-495 live shots" cited elsewhere in this codebase's own playtest notes - potentially tens of ms/sec aggregate), but the `free()` measurement itself was notably noisy (0.25ms to 22ms across otherwise-identical trials, most likely Godot's own deferred-cleanup/GC timing rather than a clean, isolated signal) - not the kind of tight, trustworthy spread this session's other benchmarks produced. Given the genuine correctness risk a pool introduces (stale `synergies`/`_handled_targets`/`collision_mask`/signal-connection state leaking between reuses) against a real-but-noisier, more modest measured upside, **not implemented without either a cleaner measurement or explicit sign-off** given the failure mode (subtle gameplay corruption, e.g. wrong damage element or friendly-fire) is more severe than this session's usual "measured slower, reverted cleanly" outcome.
   - **Projectile pooling: built for real, verified correct, measured SLOWER, reverted (2026-08-03).** User explicitly chose to attempt it anyway ("try it anyway, betting it's still worth it") despite the inconclusive scoping above, same spirit as the BossBrain bet. Plan at `C:\Users\Utility\.claude\plans\effervescent-drifting-kazoo.md`. Built `scripts/core/ProjectilePool.gd` (acquire/release free-list, capped at 300) and scoped what's safely reusable on a pooled `Projectile`: the outer node, its lifetime `Timer`, and its `VisibleOnScreenNotifier2D` (guarded create-once in `_ready()`, reused via `request_ready()` on reacquire) - `visual_node`'s synergy-dependent subtree still gets torn down and rebuilt every reuse regardless, since it can't be cheaply shared across dissimilar shot compositions. Fixed two real bugs found along the way, both kept regardless of pooling's fate: (1) `_lightning_hops_left`/`_max` were only ever reset inside the `if r_ltg_stats > LIGHTNING_BLINK_MIN` branch with no `else` - a reused non-lightning shot silently inherited a previous lightning shot's leftover hop count; (2) `_build_visuals()` unconditionally did `visual_node = Node2D.new(); add_child(visual_node)` with no teardown of a prior `visual_node` - a reused projectile with a changed synergy composition rendered BOTH the stale old-composition visuals and the new ones simultaneously (caught by the new `ProjectilePoolVisualCheck.gd`, fixed via `remove_child()`+`queue_free()` on the old `visual_node` before building the new one). Also found a `ratios`/`total_power` leak in `_ready()`'s normalization loop (only ever sets/accumulates, never resets) via `ProjectileReuseMechanicsCheck.gd`, fixed in a new `_reset_pooled_state()` that runs at the top of every `_ready()`. State-leak correctness verified via `ProjectilePoolStateLeakCheck.gd` (real accumulated combat state - a real `_handle_hit()`, decremented lightning hops, a cached homing target - cleanly reset across a release+reacquire cycle with a different synergy composition, 6/6 assertions pass) and `ProjectilePoolVisualCheck.gd` (reused visuals reflect only the new composition, no duplicate subtree). `ProjectileBroadphase` integration confirmed correct by code inspection rather than a redundant test - it's registered/unregistered from the exact same unconditional `_ready()`/`_exit_tree()` call sites as `ProjectileManager`, which the smoke check already proved cycles correctly. **The decisive same-process interleaved A/B** (`ProjectilePoolPerfCheck.gd`, 200-projectile batches, 6 rounds alternating plain/pool order, round 0 discarded as warmup): pool `acquire()`+`release()` measured a clean, consistent **12.0% SLOWER** than plain `new()`+`queue_free()` - every one of the 5 measured rounds was slower, not just the average, so this isn't noise. `_build_visuals()`'s per-shot node construction dominates the real cost and isn't pooled, so there was never much left for the pool to save; the acquire/release bookkeeping (free-list pop/push, `request_ready()`, `remove_child()`) is net overhead on top. Worth noting the benchmark was already the pool's *best case* - it manually called `release()` every cycle for 100% reuse, but the real wiring's self-release path (`Projectile._release_or_free()`) was never actually finished to call `ProjectilePool.release(self)` (it only ever did `queue_free()`), so real gameplay would have gotten 0% reuse and paid the acquire overhead for nothing. **Reverted**: `HexTile._fire_combined_projectile` back to plain `_ProjectileClass.new()`; `ProjectilePool.gd` left in the tree unused (same `packet_tax.rs`/BossBrain precedent). Kept in place as low-overhead, already-tested infrastructure: the B1/B2 leak fixes (real bugs independent of pooling), the reusable Timer/VisibleOnScreenNotifier2D guards in `_ready()`, and `_reset_pooled_state()` - none of it is wired to anything pooling-specific anymore, but unwinding it for a fraction-of-a-microsecond saving had no measurable benefit and real risk of reintroducing the bugs it fixed. Full regression suite (`TestRangeCheck`, `HitDecayCheck`, `TileConfigCheck`, `MapConnectivityCheck`, `StatusEffectLodCheck`) re-run clean after the revert.
   - **Projectile broadphase spatial hash: shipped 2026-08-03.** Real Big-O analysis (prompted by a friend's bad-performance playtest report) identified `ProjectileBroadphaseRs.query_hits()`'s flat O(projectiles x targets) double loop as a genuinely quadratic-in-enemy-count term (targets scale with enemies x ~6 parts, projectiles scale with how many mechs are firing) - the exact thing the module's own original comment deferred "until profiling says it's needed." Rewrote it as a grid-bucket spatial hash (same `HashMap<(i64,i64), Vec<usize>>` idiom `hexgrid_sim.rs` already uses, adapted to continuous-space cells, `CELL_SIZE = 150.0`): targets bucketed by position once per call, each projectile only tests candidates from cells its swept-segment bounding box overlaps. Verified exact-correctness via the existing `ProjectileBroadphaseParityCheck.gd` (unchanged, still passes - the spatial hash is an implementation detail behind the same signature/contract) and `TestRangeCheck.gd`. Measured via a clean direct-timing benchmark (`Time.get_ticks_usec()` bracketing single calls, no physics-frame/game-loop noise - the trap that burned every TIME_PHYSICS_PROCESS-based diagnostic earlier this session): **2.2x speedup (53.5% faster)** at 900 targets x 400 projectiles, exact pair-for-pair match against the old flat loop confirmed on the same dataset first.
   - **Separation-steering batching: shipped 2026-08-03.** Same Big-O pass found `Mech._compute_separation()` - a `PhysicsShapeQueryParameters2D` circle query, individually timer-staggered per mech at 5Hz (`SEPARATION_QUERY_INTERVAL`) - genuinely O(n x k) where k trends toward n in a dense cluster (exactly what combat produces around the player). Replaced with a new autoload `SeparationBatcher.gd` (registered in `project.godot`, mirrors `ProjectileManager`/`ProjectileBroadphase`'s register-nothing/poll-`EntityCache`/one-batched-call pattern) + `rust_ext/src/separation.rs` (`SeparationRs.batch_compute_separation`, same grid-bucket idiom as the broadphase fix, cell size = separation radius so only the 3x3 neighborhood needs checking). Old `Mech._compute_separation()`/`_separation_query_timer` deleted (confirmed zero other callers first). **Unlike the broadphase fix, this is NOT byte-identical parity** - the old approach used a real physics-server shape query (mech bodies are ~40px-wide rectangles), the new one compares mech origin points directly (`EntityCache.get_group("enemy")` positions, since non-player mechs are on collision layer 4 AND in group "enemy" from the same `_ready()` block - an equivalent population source without a physics round-trip). A sanity check on 20 mechs in a dense cluster found this approximation diverges from the old shape-query result by up to ~0.28 (push vectors are unit-ish, 0-1 magnitude per axis) for individual mechs - a real, honestly-disclosed behavior change in a soft steering heuristic, not a bug, and an acceptable trade for eliminating the physics-server dependency entirely. Measured via the same direct-timing methodology as the broadphase fix (60 mechs, dense cluster, 20 trials after 8 warmup, no game-loop noise): **7.4x speedup (86.6% faster)** - bigger than the broadphase win because this eliminates the physics-server round-trip outright rather than just improving the algorithm around it. `MechPhysicsCostDiagnostic.gd`'s `_diag_skip_separation` flag still works (checked at the batch-collection step).
   - **Packet-tax Rust port: built, verified correct, measured SLOWER, reverted (2026-08-03).** Built `rust_ext/src/packet_tax.rs` (`PacketTaxRs.batch_scale_packets`) to batch the tax/jamming/ambush scaling `_shoot_impl`/`_fire_combined_projectile` do per armed mount, wired it into `_shoot_impl` with a two-pass collect-then-batch restructure. Correctness verified via `PacketTaxParityCheck.gd` (Rust == GDScript fallback exactly, including the no-input-mutation contract - Dictionary is a reference type across the FFI boundary same as GDScript, so the Rust side builds a fresh output dict rather than mutating the caller's synergies dict, which would otherwise corrupt the persistent precalculated packet reused across future shots). But a naive before/after comparison across separate process launches was misleadingly favorable/unfavorable in both directions due to JIT/cache warmup noise (same trap as `ProjectileBroadphaseProfileDiagnostic.gd`/`MechPhysicsCostDiagnostic.gd` earlier) - a properly-controlled test (same process, independent fresh mech sets per config to avoid consolidation-state carryover, 10 interleaved trials with the first 5 discarded as warmup) gave a clean, trustworthy answer: **Rust is ~16% SLOWER** than plain GDScript at this call site's real batch size (1-2 armed mounts per `_shoot_impl()` call) - FFI/array-marshalling overhead exceeds the saved arithmetic below that scale. Reverted the `_shoot_impl` wiring back to the plain single-pass GDScript loop (unchanged from Phase 3). Left the Rust module + parity check in the tree, unused, since the same function would very plausibly pay off if a future redesign batches ALL mechs' fire requests once per frame (mirroring `ProjectileManager`/`ProjectileBroadphase`'s proven per-frame-batch pattern) instead of once per mech - that's a bigger, riskier restructure (deferred/async firing semantics) not attempted here. **Lesson reconfirmed a third time this session**: Rust wins at real batch scale (hundreds of items/frame), not for 1-2 item calls - don't reach for a Rust port before confirming the call site can actually batch at that scale.
   - **Batch-firing redesign (to make packet_tax.rs pay off): scoped 2026-08-03, recommend against.** Would require splitting `_shoot_impl()`'s synchronous fire-and-spawn flow into two phases - mechs enqueue fire-intent requests during their own tick, a new autoload resolves every pending request in ONE batched Rust call, projectiles spawn from the results afterward (mirroring `ProjectileManager`/`ProjectileBroadphase`'s proven pattern). Real architectural risk: touches `PlayerController.gd`'s mouse-fire handling (risks a frame of input latency on PLAYER shots specifically - the kind of thing that's actually felt, not just measured), the cloak-break-on-fire/heat-venting/weapon-charge-decrement ordering, and has to coexist with `HexTile._fire_combined_projectile`'s own existing volley-consolidation-tier system without conflicting with it. Checked what it would actually buy first: the tax-scaling code itself (`_shoot_impl` lines ~1475-1483, plus `_get_ambush_multiplier`/`_apply_synergy_jamming`) is trivial - a handful of float multiplies over 1-4 synergy keys, no hidden physics/query cost. It's a small slice of the `_fire_combined_projectile` hot path already measured this session at ~61us/shot total, of which TWO full optimization passes on the bigger pattern/merge-math piece only recovered ~5% combined (~0.061ms/shot) - tax-scaling alone is a smaller fraction still, realistically low-single-digit microseconds/shot. Even at an extreme 500 shots/sec, perfect batching saves at most a couple ms/sec - nowhere near the scale of this session's real wins (e.g. the flight-batch marshalling fix was ~1.5ms/tick at just 500 live projectiles). Small theoretical ceiling, real risk, latency-sensitive on the player-facing path: **not worth building.** `packet_tax.rs`/`_batch_scale_packets_fallback` stay exactly where they are (already in the tree, unused) - this is a considered decision, not a "not started yet."
   - **Also shipped this session as a quick, well-evidenced, low-risk win** (not a Rust port): `MechStatusBars.gd` now gates its `_draw()`/`queue_redraw()` on true on-screen visibility (`VisibleOnScreenNotifier2D`, same pattern `Projectile.gd` already used for off-screen culling) instead of redrawing unconditionally on every hp/shield change - deliberately screen-visibility-based, not a distance radius, so it doesn't affect long-range Kinetic engagement of on-screen-but-distant targets.
   - **AI-tactics Rust cutover (in progress, 2026-08-03) - plan at `C:\Users\Utility\.claude\plans\effervescent-drifting-kazoo.md`:** a 5-phase plan targeting `Mech._execute_ai_tactics` (BossBrain, SightAndSearch, the flee/wild state machine, and MapGenerator's shared flow-field), sequenced safest-and-best-evidenced first. **Phase 0 (instrumentation) complete, real numbers in** via new `Phase0AiPerfCheck.gd`/`.tscn` (direct `Time.get_ticks_usec()` bracketing of the real production calls, 1-warmup-trial-discarded, matching this session's proven methodology): 
     - `MapGenerator._rebuild_flow_field`'s bounded BFS costs **6.82ms per rebuild**, on a 0.4s cadence - a genuine periodic ~7ms stutter, not a rounding error, and plausibly part of what real playtests have read as general "stuttery" feel. **This justifies Phase 3** (porting the BFS rebuild to Rust) - proceeding.
     - `BossBrain._pick_retreat_dir`'s 5-raycast fan costs only **~5.13 microseconds per call** - confirms the single-boss-population risk flagged before Phase 4 even started (same batch-of-1 shape `packet_tax.rs` lost on): this specific raycast fan is far too cheap on its own to be worth an FFI round-trip. Phase 4 (BossBrain, proceeding anyway per explicit user choice - "port it anyway, betting it'll still be faster") will need its real win, if any, to come from the enrage/ability-selection/state-machine math instead, not this raycast swap - or it will likely repeat `packet_tax.rs`'s outcome.
     - New counters added: `Mech._perf_sight_raycast_usec` (isolates just the raycast fraction of the existing `_perf_sight_usec`, for Phase 2), `Mech._perf_flow_field_rebuild_usec` (the rebuild itself, distinct from the existing `_perf_flow_field_usec`'s O(1) per-mech lookup), `Mech._perf_boss_retreat_raycast_usec`.
     - **Phase 1 (shared flat obstacle grid + Rust grid-LOS primitive) complete, 2026-08-03.** Investigating the real raycasts surfaced a genuine finding: every sight/search-avoidance/boss-retreat raycast in the codebase (`SightAndSearch.gd:79,197`, `BossBrain.gd:124`, `Mech.gd:2467`) passes `collision_mask=1` ("Env" layer - only the four outer map-boundary walls, per `MapGenerator._create_wall_collision`), NOT layer 32 ("Obstacles" - trees/boulders/ruins/corpses). So in shipped gameplay today, mechs can already see and retreat-plan straight through solid terrain - only the literal map edge ever blocks these checks. Consistent across all 4 independent call sites, so read as a deliberate simplification rather than a copy-paste bug - but it meant Phase 1 as originally scoped (replicate real obstacle occlusion in a batched grid-march) had no real occlusion to replicate. **Put to the user directly**: port the boundary-only quirk as-is, add real obstacle occlusion alongside the port, or drop the shared grid-LOS primitive entirely. **User chose to add real obstacle occlusion** - a deliberate gameplay behavior change, not a pure parity port: mechs will genuinely stop being able to see/path through solid terrain once Phase 2/4 wire this in. Built `rust_ext/src/solid_grid.rs` (`SolidGridRs`, Amanatides & Woo DDA grid-march against a flattened obstacle-solidity buffer, endpoints never tested so a mech/target standing on obstacle geometry doesn't self-block) + `scripts/core/SolidGridBatcher.gd` (autoload, full-map buffer - not a moving window like the flow field, since sight/retreat checks happen anywhere combat is - rebuilt only when `obstacles.size()` changes, piggybacking on obstacle destruction being rare rather than adding a new invalidation signal; `ClassDB.class_exists` gate + GDScript fallback of the identical DDA algorithm, per this codebase's established contract). Verified via new `SolidGridLosCheck.gd` (7 hand-built cases against a synthetic wall, Rust vs. fallback vs. hand-computed expected result all agree) and a real-Normal-map integration smoke test in `Phase0AiPerfCheck.gd` (2886 real obstacle cells, 400x250 map: grid rebuild costs 1.058ms, one-off on obstacle destruction, not per-tick). `MapConnectivityCheck.gd` re-run clean as an adjacent-regression tripwire. Nothing consumes this yet - `SolidGridBatcher.batch_line_of_sight` exists and is verified correct, but no live AI code path calls it until Phase 2/4 wire it in, so no player-visible behavior has changed yet.
     - **Phase 2 (sight-check gate batch port) complete, 2026-08-03 - measured, real win.** Scoped narrower than the plan's full bullet list on purpose: only the sight-check GATE (range test + Phase 1 grid-LOS, replacing `SightAndSearch._update_player_sight`'s per-mech raycast) shipped this pass. The expanding-square search-PATTERN state machine (`_execute_search`/`_advance_search_leg`/`_pick_frontier_point`/etc. - a dozen+ stateful fields, 3 separate RNG call sites, nested per-attempt obstacle-avoidance raycasts) is real, separate, higher-risk follow-up work, deliberately not bundled into the same diff - the plan's own Phase 2 description already lists these as two distinct bullets, so this is a sub-phase split, not a scope cut. New `scripts/core/SightAndSearchBatcher.gd` (autoload, mirrors `SeparationBatcher.gd`'s shape) replaces every mech's independently-staggered `SIGHT_CHECK_HZ` timer with one shared cadence, does the (too-cheap-to-batch, same packet_tax lesson) range gate in plain GDScript, then calls Phase 1's already-built `SolidGridBatcher.batch_line_of_sight` once for every in-range mech - **no new .rs module needed for this sub-phase**, the entire Rust-batching win comes from reusing Phase 1's grid-LOS primitive instead of duplicating it. `Mech._execute_ai_tactics` no longer calls `_update_player_sight` itself, just reads the already-fresh `has_sight_of_player`/`last_known_player_pos` the batcher maintains (`_update_player_sight` itself is untouched - `JammerWiringCheck.gd` still calls it directly to force past the throttle). Verified via new `SightAndSearchBatcherCheck.gd` (real Mech/MapGenerator/JammerField instances, deterministic obstacle wall: clear-LOS-in-range, wall-blocked-in-range, out-of-range, and blind-field cases all pass) plus the existing `SearchEscalationCheck`/`FleeThresholdCheck`/`DirectorTellsCheck`/`JammerWiringCheck`/`TestRangeCheck`/`TileConfigCheck` all re-run clean. Measured via `MechPhysicsCostDiagnostic.gd`'s existing Config X (60 mechs, real clustered combat): sight-check cost dropped to **0.033 us/mech-tick** (0.3% of total AI-tactics cost) - eliminating the per-mech physics-server round-trip in favor of one batched pure-Rust grid-march call, same category of win as separation.rs's 7.4x (this one wasn't independently isolated before/after in this exact diagnostic, but the mechanism is identical: N physics-server calls collapsed to 1).
     - **Phase 2 continuation (search-pattern state machine) - measured, deliberately deferred, 2026-08-03.** Before committing to a full Rust port of `SightAndSearch._execute_search`/`_advance_search_leg`/`_pick_frontier_point`/etc. (a dozen+ stateful fields on `Mech.gd`, 3 separate RNG call sites, nested per-attempt obstacle-avoidance raycasts, and `Squad.explored_cells` queries not yet even read), added a `Mech._perf_execute_search_usec` counter and a dedicated diagnostic (`Phase2SearchPatternPerfCheck.gd` - real MapGenerator + obstacle wall keeping mechs in the "near" LOD branch with LOS genuinely blocked, forcing persistent SEARCH state). First attempt reused one live mech population across all 5 trials and got a misleadingly monotonic-decreasing total (31->29->25->16->11ms) - mechs migrate outward as their search pattern escalates, drifting out of the test condition over the run, the same state-carryover trap `packet_tax.rs`'s first flawed A/B test hit earlier this session. Fixed with independent fresh mech sets per trial (same fix that worked there): stable result, **1.787 us/mech-tick**. Real, but a meaningfully smaller and more diffuse signal than every other port this session (sight-check eliminated a real physics-server round-trip entirely; this is mostly already-cheap unthrottled GDScript arithmetic with a small occasional raycast cost mixed in) - and the implementation risk (that dozen+ stateful field set, RNG-boundary correctness, squad-query semantics) is real. **Deliberately deferred** rather than attempted: Phase 3's flow-field number (7.6ms every 0.4s, a genuine periodic stutter) is a cleaner, bigger, lower-risk win available right now - moved there instead. Flagged as a real future candidate, not abandoned.
       - **Follow-up, 2026-08-03: closed a real gap left over from Phase 1/2, full state-machine port still not worth it.** `_next_leg_target()` (called from `_start_search_pattern`/`_advance_search_leg` on every leg transition) was STILL doing real `PhysicsRayQueryParameters2D` obstacle-avoidance raycasts - Phase 2 only ever wired the sight-check GATE up to `SolidGridBatcher`'s grid-marched primitive, this call site was missed even though Phase 1's own header comment explicitly lists "search-leg avoidance" as an intended consumer (a nearby `_execute_ai_tactics` comment even claimed the whole state machine had "no FFI cost to save," which was true for everything except this one raycast). Redirected to `SolidGridBatcher.batch_line_of_sight` (same disclosed grid-vs-physics approximation tier already accepted for the sight check) - zero new Rust code needed, reused already-verified infrastructure. Verified via `SearchEscalationCheck`/`SightAndSearchBatcherCheck`/`FleeThresholdCheck` all re-run clean. **Re-measured with `Phase2SearchPatternPerfCheck.gd`: 1.7827 us/mech-tick, statistically unchanged from the pre-fix 1.787.** Root cause: `_next_leg_target()` only fires on leg TRANSITIONS (a handful of times per mech over 5 sim-seconds), not per-tick like the sight check was - the aggregate raycast cost was always a small fraction of the state machine's total, so eliminating it doesn't move the number. The fix ships anyway (closes a real architectural gap, zero measured downside, matches the codebase's established direction), but it reconfirms rather than overturns the original verdict: **the search-pattern state machine as a whole is still not worth a full Rust port** - it's genuinely cheap unthrottled GDScript arithmetic, and the one real physics-server cost inside it was never big enough to be the deciding factor either way.
     - **Phase 3 (flow-field BFS rebuild) shipped, 2026-08-03 - measured, real win.** Justified by Phase 0's own number (6.8-7.6ms per rebuild, once per 0.4s - a genuine periodic stutter). Unlike Phase 1/2 (soft heuristics, disclosed approximations accepted), this is navigation-affecting, so it required byte-identical parity, not just "close enough." Built `rust_ext/src/flow_field.rs` (`FlowFieldRs`), deliberately NOT reusing Phase 1's `SolidGridRs` LOS buffer - checking `MapGenerator._build_navigation` directly found `astar_grid` marks BOTH `obstacles.has(cell)` AND `terrain==WATER` solid, while Phase 1's LOS buffer is obstacles-only; sharing one buffer would have silently made water passable for routing (or blocked sight through water, neither ever reviewed) - kept as its own separate solidity buffer with the matching union definition instead. `MapGenerator._rebuild_flow_field` now dispatches to Rust (`ClassDB.class_exists` gate) or the identical GDScript BFS fallback (`_rebuild_flow_field_fallback`, unchanged logic, same neighbor-offset iteration order preserved in Rust for exact tie-break parity). Verified via new `FlowFieldParityCheck.gd` - byte-identical across 4 map types x 3 target cells each (Forest/Volcano/Normal/Tabletop, ~2600-3250 cells per field, one legitimate solid-target skip) - plus `MapConnectivityCheck.gd` re-run clean. Measured via the same `Phase0AiPerfCheck.gd` diagnostic that produced the original baseline: **7.6ms -> 1.0ms per rebuild, a 7.5x speedup** (trial 1 correctly excluded as warmup - it includes the one-off solidity-buffer build cost, same convention as every other benchmark this session).
     - **Phase 4 (BossBrain) - built, verified correct, measured SLOWER, reverted (2026-08-03).** The riskiest bet in the plan: boss population is confirmed always exactly 1 (`Main.gd:972`), the same batch-of-1 shape that sank `packet_tax.rs` earlier this session - the user explicitly chose to attempt it anyway ("port it anyway, betting it'll still be faster") rather than skip it. Extended `rust_ext/src/solid_grid.rs`'s `SolidGridRs` with `march`/`batch_probe_clearance` (returns grid-marched clearance DISTANCE, not just a boolean - `_pick_retreat_dir` needed "how far until the first obstacle," which Phase 1's LOS-only primitive didn't expose) and wired `BossBrain._pick_retreat_dir`'s 5-raycast fan to call it via `SolidGridBatcher.batch_probe_clearance` (one batched call instead of 5 individual `PhysicsRayQueryParameters2D` queries). Verified correct: `SolidGridLosCheck.gd` extended with 4 hand-computed clearance cases (Rust == fallback == hand-verified geometry, exact), plus a wiring-correctness integration check confirming a boss backed against a wall picks the most-open retreat direction. **Measured honestly, twice**: a naive cross-process before/after comparison (8.15us/call after vs. Phase 0's 5.13-7us/call before, across separate process launches) was inconclusive by this session's own established standard - cross-process timing is exactly the noisy methodology already proven unreliable earlier (JIT/cache warmup contamination). Built a proper same-process interleaved A/B instead (`Phase4RetreatDirABCheck.gd`: both the reverted-real-raycast-fan and the Rust-batched version reimplemented side by side, 10 trials/config alternating order, first trial discarded as warmup) - clean, tight, trustworthy result: **Rust-batched is 59.4% SLOWER** (5.16us/call real-raycast-fan vs. 8.23us/call Rust-batched). FFI/Dictionary-marshalling overhead for 5 queries exceeds the cost of 5 already-fast native `PhysicsServer2D` raycasts at this population size - the `packet_tax.rs` lesson repeating exactly as the evidence predicted at every stage of this investigation (Phase 0's own 5-7us baseline had already flagged this piece as too cheap to be worth an FFI round-trip). **Reverted** `_pick_retreat_dir` to the original real-raycast fan; left `SolidGridRs.march`/`batch_probe_clearance` + `SolidGridBatcher.batch_probe_clearance` in the tree unused (same precedent as `packet_tax.rs`), in case a future full-frame-batch redesign finds a genuinely batchable call site. Deleted `BossBrainRetreatCheck.gd`/`.tscn` - it validated the now-reverted Rust path specifically and would otherwise sit in the tree testing dead code. `update_enrage`'s threshold-crossing and `_start_ability`'s ability-selection (the plan's other two candidate pieces) were NOT separately built-and-measured: both are called even less frequently and with even less per-call compute than the retreat-clearance probe (which was itself already the single largest, most plausible-to-batch piece Phase 0 identified) - building and measuring them would very likely just reproduce the same regression with less signal, for real additional risk/effort. **Overall Phase 4 verdict: real attempt made, honest result is a loss, matches the plan's own stated expectation.**
     - **AI-tactics Rust cutover plan: closed out, 2026-08-03.** Phases 0-3 shipped real, measured wins (sight-check gate: physics round-trip eliminated; flow-field rebuild: 7.5x, 7.6ms->1.0ms, byte-identical; search-pattern state machine: measured at 1.79us/mech-tick, deliberately deferred as lower-value/higher-risk than the alternatives). Phase 4 (BossBrain) was genuinely attempted per the user's explicit choice and honestly reverted after rigorous measurement. Full plan at `C:\Users\Utility\.claude\plans\effervescent-drifting-kazoo.md`.
5. **Feature batch:** Bot League auto-pilot spectating (entered via Frank's exhibition table), Tactical Puzzle Challenges (Frank's chess-style JSON scenarios, restricted parts, strict timers), Shop events (wager waves first; Shop Cat flagship later), shop-framing UI paradigm pass across all menus. Champion Card art compositing the real mech sprite: shipped. Zero code exists yet for Bot League/Puzzle Challenges/Wager Waves/Shop Cat - fully greenfield.
6. **Data-driven Map/Campaign Editor + Multiplayer** (captured 2026-07-27, design-only, not started): a standalone editor tool for building maps/campaign content that does NOT need updating every time game code changes - editor and game should share one common index/schema (tile types, stats, biome/terrain definitions, etc.) that the editor reads live from the game's own data (mirroring how `TileStatsRegistry`/`res://tiles/<TileType>/stats.json` already decouples balance data from code) rather than hardcoding a duplicate schema in the editor. Synchronous multiplayer (GodotSteam, see Future Expansions below) is a related long-term wishlist item, tracked separately there.

---

## 3. Backlog & Improvements

### Bugs & Code Health
- **GDScript & Engine Performance Audit (2026-08-01):** Comprehensive review of game scripts identified 7 performance chokepoints, ordered from easiest to hardest to fix:
  1. *Integer-to-String Conversions in Tile Dictionaries (Easiest, ~2m):* `MissileRackTile.gd` (`str(entry_direction)`) and `OrbitingArrayTile.gd` (`str(cell_idx) + ":" + str(dir)`) allocate strings every energy packet. Use direct `int` keys instead.
  2. *Bypassing EntityCache in Managers (Very Easy, ~5m):* `Main.gd`, `CameraShake.gd`, and `MinimapOverlay.gd` invoke `get_tree().get_nodes_in_group(...)` directly instead of using `EntityCache.get_group(...)`.
  3. *Per-Packet Array Literals in TileStatsRegistry (Easy, ~10m):* `TileStatsRegistry.get_stat_by_rarity()` receives inline array literals (`[1.0, 1.2, ...]`) inside `process_energy()`. Replace with static script constants.
  4. *Transient Physics Query Allocations in Mech AI (Medium-Easy, ~15m):* `Mech._compute_separation()` and strafe raycasts allocate new `PhysicsShapeQueryParameters2D`, `CircleShape2D`, and `PhysicsRayQueryParameters2D` objects per query instead of reusing member properties.
  5. *Homing Missile Spatial Query Phase Staggering (Medium, ~20m):* Homing missiles (`Projectile.gd`) query spatial target checks simultaneously on spawn. Add randomized initial phase offsets (`_homing_timer = randf_range(...)`) to stagger queries across frames.
  6. *Hazard Damage Check Throttling (Medium-Hard, ~30m):* `JumpjetResidue.gd` and `OilSlickHazard.gd` execute overlap/DoT checks every frame at 60 Hz across all enemies. Throttle checks to 5–10 Hz via delta accumulation.
  7. *Canvas Draw Call Batching for MechStatusBars (Hardest, ~45-60m):* `MechStatusBars.gd` issues multiple `draw_rect()` canvas calls per visible mech. Refactor custom `_draw()` into NinePatchRect / TextureProgressBar UI nodes or a custom shader material.
  - **Follow-up pass, 2026-08-03 - items 1 and 6 shipped, items 2/4/5 found already resolved, a real "can never fire" bug found along the way.** Checked each item's current state before touching anything (several turned out to predate/be superseded by tonight's other work):
    - **Item 1 (int/string keys): shipped.** `MissileRackTile.gd`'s `face_key` is now the raw `entry_direction` int; `OrbitingArrayTile.gd`'s is now `Vector2i(cell_idx, entry_direction)` (natively hashable, no string concat) - both were purely-internal accumulator keys, never displayed/parsed/serialized as text (confirmed via a full grep of every `_face_magnitudes` reference, including `RustGridSim.gd`'s separate `LanceMountTile`-specific composite-string key, which was left untouched since it's a different tile with a different cross-file contract). Verified via new `CapitalWeaponFaceKeyCheck.gd` (repeated-hit accumulation on one key, distinct keys staying distinct, correct fed-face counting and gating) plus `TestRangeCheck`/`TileConfigCheck`/`DataDrivenTilesCheck` re-run clean.
    - **Real bug found while building that check, fixed regardless of the key-type change**: `MissileRackTile.gd`/`OrbitingArrayTile.gd` both called `packet.clone()` when updating `_fed_packet` - `EnergyPacket` has no `clone()` method (only `.copy()`, confirmed via `LanceMountTile.gd`'s own identical `_fed_packet` line already using `.copy()` correctly). The resulting "Invalid call" script error aborted `process_energy()` before it ever reached the `ready_to_fire = true` line, on every single call - meaning **both capital weapon tiles could never actually fire in real gameplay**, the exact same failure class as the earlier "Lance Beam can never fire" fix, just undiscovered until a regression check finally exercised this exact code path directly. Fixed both call sites to `packet.copy()`; `CapitalWeaponFaceKeyCheck.gd`'s `ready_to_fire` assertions went from FAIL to PASS immediately after.
    - **Item 6 (hazard damage throttling): `JumpjetResidue.gd` shipped, `OilSlickHazard.gd` found already throttled.** `OilSlickHazard._tick_damage()` already only runs on its own 0.5s (2Hz) `BURN_TICK_INTERVAL`, not every frame - no changes needed. `JumpjetResidue._physics_process()` was genuinely still calling `get_overlapping_bodies()` (a real physics-server round-trip) and applying damage unconditionally every physics tick (60Hz) per live zone. Throttled to 10Hz (audit's own suggested 5-10Hz range) via the same elapsed-accumulator pattern as the Simulation LOD status-effect throttling - `damage_per_sec` scales by the real accumulated elapsed time each fire, so total damage over a zone's lifetime is unchanged, only chunked into 1/6th as many hits. Verified via new `JumpjetResidueThrottleCheck.gd`: a real `Mech` target sitting in a real zone for its full 3s lifetime took 29.17 damage against an expected 30.0 (damage_per_sec x lifetime), within one throttle interval's tolerance - confirmed no drift/leak, not just "didn't crash."
    - **Items 2, 4, 5: found already resolved or not actually hot, no changes made.** Item 2 (`get_nodes_in_group` instead of `EntityCache.get_group`): every cited call site turned out to be a one-off event handler (player death, map rotation, mouse-wheel zoom-to-strategic), not a per-frame hot path - `Main.gd`'s own `_update_player_blind_state` comment already documents an earlier pass that gated its "enemy" group scan behind an actual state-transition instead of running every frame; `MinimapOverlay.gd`'s occurrences are already behind its existing 12Hz redraw throttle. Converting these to `EntityCache` would be correct but measurably pointless. Item 4 (`Mech._compute_separation()` transient allocations): the function itself no longer exists - fully replaced by `SeparationBatcher.gd`/`separation.rs` earlier this session (7.4x win, see above); the remaining strafe-raycast `PhysicsRayQueryParameters2D.create()` call is a single ray per engaged-mech-tick, too cheap on its own to be worth the reuse-a-member-var micro-optimization (same "not worth it below a real threshold" lesson `packet_tax.rs`/Phase 4 already established). Item 5 (homing missile phase staggering): already implemented - `_homing_query_timer = randf() * HOMING_QUERY_INTERVAL` exists at both `_ready()` and `_reset_pooled_state()`, confirmed via direct code read.
    - **Item 7 (MechStatusBars draw-call batching): measured, NOT a bottleneck, no refactor needed.** Before investing in the suggested `_draw()`-into-NinePatchRect/shader rewrite (a real, bigger, riskier rendering-pipeline change), measured the existing `_perf_draw_usec` counter (already accumulated by `_draw()` itself, already read/reset once a second by `FpsCounter.gd` in real gameplay - no new instrumentation needed) via a throwaway synthetic scenario: 80 status-bar instances, each forced through 20 rounds of real hp/shield change (same `_process()` change-detection trigger real gameplay uses). Result: **2.475us per `_draw()` call** - even the unrealistic worst case of all 80 mechs redrawing in the exact same frame (the existing change-detection + on-screen gates mean this essentially never happens in practice) comes to only **0.198ms**. `draw_rect()`'s CPU-side dispatch cost is inherently tiny (6 calls per redraw: panel bg/border, hp bg/fill, shield bg/fill) - this was never going to be a real bottleneck, and the earlier "1727 draws"/"heavy ADD-blended transparent glow stacking" finding elsewhere in this doc already points at projectile visual overdraw (a GPU fill-rate cost `_perf_draw_usec` doesn't even measure) as the actual likely culprit for any real frame-time cost, not status bars. **Audit item 7 closed out as measured-and-not-worth-it**, matching the "don't chase a cost below a real threshold" lesson this session already established for strafe-raycast reuse and several Rust-port attempts.
  - **All 7 GDScript & Engine Performance Audit items now resolved** (2 shipped, 1 bug fixed along the way, 4 found already resolved/not real bottlenecks after measurement). Audit closed out.
- **Rendering-overdraw investigation: MEASURED, and it's NOT the bottleneck - real finding is per-projectile physics-tick cost at scale (2026-08-03).** This whole thread traced back to the same stress-test video referenced throughout this doc (wave 12, 495 live shots, 34 enemies, FpsCounter reading "1727 draws" during a visible 1-2fps collapse) and had been flagged repeatedly as the likely real culprit, but never actually measured. First real obstacle: `Performance.get_monitor(RENDER_*)` reports all zeros under `--headless` (the dummy rendering driver doesn't track them, and `RenderingServer.frame_post_draw` hangs forever waiting for a signal that never fires under it - confirmed via `HeadlessRenderStatSanityCheck.gd`). Every other perf check this session used headless direct-timing; this one required real windowed Godot runs instead. Investigation, via `ProjectilePhysicsAtScaleCheck.gd` (kept in the tree, unlike this session's other one-off benchmarks, as the baseline to re-run once real work starts on the actual finding below):
  1. 1500 real `Projectile.gd` instances (mixed random synergy compositions) produced ~1800-1900 draw calls - closely matching the historical "1727 draws" figure once ~34 enemies' own visuals are accounted for. Looked like confirmation.
  2. First frame-time comparison came back pinned at exactly 60fps in BOTH the empty-baseline and loaded case - a vsync ceiling masking any real cost, not evidence of zero cost. Disabled vsync for an honest uncapped reading.
  3. Uncapped: empty baseline ~2200-2600fps, 1500 dense on-screen projectiles ~30-55fps (noisy across runs but consistently large) - a real, large frame-time cost. Rendering looked confirmed a second time... except:
  4. The SAME 1500 projectiles spread across a real map-sized area (mostly off-screen/culled by Godot's automatic per-CanvasItem viewport culling - draw calls dropped ~15x, from ~1830 to ~90) still cost almost exactly the same frame time. Draw-call count varies wildly between the two scenarios; frame time does not. That rules out rendering/draw-call volume/overdraw as the driver.
  5. `Performance.TIME_PHYSICS_PROCESS` stayed ~constant (~26-32ms) across both draw-call extremes, closely tracking total frame time - pointing at physics, not rendering. Confirmed directly: the same 1500 spread-out projectiles with `set_physics_process(false)` dropped frame time **8.9-13x** (down to 1.9-3.4ms, close to the ~0.5-2.3ms empty baseline).
  **Verdict: rendering/overdraw is a closed, measured non-issue.** The real cost at high live-projectile counts (1500, plausible in an intense multi-mech firefight - well beyond the "100+" scale this codebase's own comments had previously flagged as "real") is each `Projectile`'s own per-tick `_physics_process` logic (movement, homing/vortex query timers, `ProjectileBroadphase.report_movement`, off-screen-notifier housekeeping) - CPU-side simulation cost, not GPU-side rendering cost. Nobody had actually measured this before; "1727 draws" was the only number anyone had looked at, and it turned out to be a red herring that merely correlates with projectile count without being the actual cost driver.
- **Physics-tick-at-scale follow-up: isolated and fixed the dominant piece, shipped 2026-08-03.** New `ProjectilePhysicsBreakdownCheck.gd` (kept in the tree alongside `ProjectilePhysicsAtScaleCheck.gd` as this investigation's ongoing measurement pair) broke down `TIME_PHYSICS_PROCESS` at N=500 (matching the real stress-test video's exact live-shot count, not the exaggerated 1500 used to first surface the problem): `Projectile._physics_process_body` itself was already lean (~1-1.25ms/tick aggregate, only ~5-8% of the tick), and toggling `ProjectileBroadphase`/`ProjectileTargetingBatcher` off individually each accounted for only ~1-2ms/tick. The single biggest piece, found by adding temporary `_perf_collect_usec`/`_perf_rust_call_usec` counters to `ProjectileManager.gd`: its own per-tick flight-batch dispatch cost **~3.0ms/tick combined** - ~1.09ms building 500 per-projectile request Dictionaries in GDScript, and **~1.94ms marshalling that Array of 500 nested Dictionaries across the Rust FFI boundary in ONE call**. That last number is the real story: this session's own established lesson ("Rust wins at real batch scale, batching eliminates per-call dispatch overhead") turned out to be incomplete - batching fixes call-COUNT overhead, but marshalling large/complex per-item payloads (a Dictionary with a nested 7-key "ratios" sub-dict, ~11 Variant key lookups per entry) across the FFI boundary still costs real time proportional to DATA volume, a distinct cost axis the earlier `packet_tax.rs`/Phase 4 investigations never isolated because they never tested at 500-item batch sizes. **Fix**: rewrote the payload shape from `Array[Dictionary]` to two packed arrays (`PackedInt64Array` instance_ids + `PackedFloat64Array` requests_flat, fixed 20-field stride per projectile) - a direct memory-copy marshal instead of per-key Variant conversion. `rust_ext/src/projectile_flight.rs` gained `compute_batch_flat` (the new hot-path entry point) and a primitive-tuple `compute_step_core_prim` shared by every entry point so the actual math isn't duplicated; the OLD Dictionary-based `compute_batch`/`compute_step` stay in the tree unchanged - `compute_step` because it's still the correct, rarely-called single-projectile fallback (Dictionary marshalling cost doesn't matter once per projectile lifetime), `compute_batch` solely as the reference implementation for the new `ProjectileFlightFlatParityCheck.gd` (12 hand-built cases spanning every synergy branch including the lightning zig-zag's instance_id-seeded hash, dict-shaped vs. flat-shaped requests built from the same underlying values, asserts every output field matches within float tolerance - all 12 pass). `Projectile.gd`'s `_prepare_flight_request`/new `_prepare_flight_request_flat` share their state-prep logic (the throttled homing-target-search branch, ratio/steering setup) via an extracted `_prepare_flight_state()` helper, so the one behavior-sensitive piece isn't duplicated between the Dictionary and flat code paths. instance_id rides in a SEPARATE `PackedInt64Array` rather than being folded into the float payload, specifically to avoid any f64-precision risk on an opaque 64-bit ID that's also used as the lightning zig-zag's hash seed. Verified: `TestRangeCheck`/`HitDecayCheck`/`TileConfigCheck`/`MapConnectivityCheck`/`FpsCounterCheck` all re-run clean; both debug and release DLLs rebuilt. **Measured, reproduced across 2 runs**: `ProjectileManager`'s collection loop dropped **~1.09ms -> ~0.81ms/tick (~26% faster)**, the Rust batch call itself dropped **~1.94ms -> ~0.40ms/tick (~79% faster, 4.8x)** - combined per-tick dispatch cost **~3.03ms -> ~1.21ms, a ~60% reduction**. Wired the new counters into `FpsCounter.gd`'s existing "per sec" breakdown line (`flight_collect`/`flight_rust_call`) so this win (and any future regression) stays visible in real playtests, not just synthetic benchmarks.
- **God-Class Aftercare (done):** `Mech.gd` audited at ~3000 lines. The 5 pre-existing splits (`BossBrain`, `StatusEffectRunner`, `PlayerController`, `MagnetSystem`, `SightAndSearch`) were confirmed genuinely lazy-constructed and cleanly delegated - never the problem. All three "equippable ability" cuts are done: `CloakSystem` (`b3adf92`), `JammerModuleSystem` (`fedc6ae`), `HealBeaconSystem` (`4c17922`) - each verified with zero regressions across the full check suite. The former 394-line `_recalculate_grid` was also broken up: unlike the ability cuts, it's the central orchestrator touching ~25 fields across every other subsystem and is called externally from `GarageSimulationRunner.gd`/`GarageTestRange.gd`/several debug checks, so moving it to a new composed file would've relocated risk rather than reduced it. Instead it stayed on `Mech` as a pure extract-method split into `_reset_grid_state`/`_compute_mass_and_stat_modifiers`/`_simulate_energy_flow`/`_collect_weapon_mounts_and_tile_capabilities`/`_finalize_grid_state` - zero behavior change, verified across 24 regression checks (Cloak/Jammer/HealBeacon/TestRange/SimTimeline/GarageEditFix/TileConfig/all Jammer variants/MagnetRepel/Synergy/WaveScaling/TileRarity/Mortar/Blueprint/FleeThreshold/SearchEscalation/DirectorTells/RustExt/Rasterizer/MapConnectivity), all passing. The **damage/combat pipeline** (`apply_damage`/`apply_part_damage`/shield mitigation/disable rolls, ~366 lines) was audited last and found already healthy: it's 8 well-named, appropriately-sized private helpers (12-75 lines each), not one monolith. `apply_damage` has 24 external callers and `_show_floating_text`/`elemental_resistances`/`recent_damage_log` are accessed by name from SquadDirector/Main/BossBrain/Drone/HealBeaconSystem/PlayerController - moving any of it to a composed file would add an indirection hop with zero reduction in Mech's public surface. Left as-is; no extraction warranted. Tech-debt pass complete.
- **Consolidation-first mode:** per direction given 2026-07-16, no new features until this pass and a general stability sweep are done. The stability sweep itself landed 2026-07-27 (see Recently Shipped) - the 127 `scripts/debug/*Check.gd` regression harnesses were also scanned for actual bitrot (broken file references) as part of that pass and came back clean (0 found; all reference real, existing files) - the "127 flat files" complaint is a discoverability/organization concern, not dead code, so none were deleted. Still open: whether to reorganize them into topic subfolders for discoverability (not done - would need every matching `.tscn`'s `ext_resource` path updated too, deferred pending explicit go-ahead given the file-count risk).
- **Spatial Hashing:** Largely superseded by `EntityCache` + the saturation tiers; the remaining need (projectile broadphase) folds into queue item 4.

### Gameplay & Balance Improvements
- **Kinetic firing-mechanics enhancement (raised 2026-07-27, explicitly deferred - "later"):** the user wants Kinetic's cross-map sniping identity to feel more distinct/deliberate (charge-up? travel-time telegraph? extreme-range lock-on assist?), separate from its existing raw-range stat (already up to 5x base range, ~7000px full-Kinetic per `Projectile.gd`'s `KINETIC_RANGE_BONUS`). Raised specifically while discussing the `MechStatusBars` off-screen LOD gate - confirmed that gate is screen-visibility-based (`VisibleOnScreenNotifier2D`), not a distance radius, so it doesn't constrain this in any way. Zero design work done - flagged for a dedicated conversation, likely alongside Heat below (a cost/risk axis could be what makes a slower, more deliberate Kinetic playstyle pay off).
- **Heat as a system (raised 2026-07-26, explicitly deferred - "later"):** the user's framing - right now the weapons look cool but there's no real challenge beyond learning basic energy routing; a Heat mechanic is the leading candidate for adding actual friction/risk to firing, and ties directly into the "elemental meta is calcified" problem above (a cost axis that hits the fast/spammy elements harder could be what makes Vortex/Poison/Ice viable again). Zero design work done - purely a flagged topic for a future dedicated conversation, not scoped at all yet.
- **Boss Variety:** Stop scaling up Brawlers. Use role-specific bosses or genuine boss-only kits.
- **Synergy Effects:** Signature status marks per element are shipped; deepen the lingering effects for VORTEX and VAMPIRIC (bleed/immobilize interactions with corpse statues).
- **Elemental meta is calcified (raised 2026-07-27):** the user's read on current play - Lightning/Explosive/Kinetic/Pierce clear the map in seconds and dominate every build; Vortex and Poison are pariahs (slow/disruptive in ways that cost more than they're worth), Ice's projectile slowdown makes it hard to justify packing at all. No fix designed yet - needs a real balance pass, likely tied to the Heat system discussion below (a universal cost/risk axis might be what's needed to make the "slow" elements pay off rather than just nerfing the fast ones).
- **Anchor tile - base rarity is a real enabler, Mythic tier is the actual gap (confirmed via code + corrected via user testing, 2026-07-27):** initial read was that ANY-rarity Anchor's `is_vortex_immune` (`Mech.gd:1760-1764`) only zeroes out your own Vortex weapon's self-pull, and since that self-pull multiplier is just 0.05x (`Projectile.gd:1364-1379`) it looked like a near-no-op - that was wrong. `pull_strength = 600 * vortex_ratio * pull_mult * magnitude_pull_mult`, and `magnitude_pull_mult` scales up to 3x with power, so even at 0.05x self-pull that's up to ~90 units/sec of sustained drag while your own packet lingers, compounding across a burst - confirmed by the user: enough to drag the player across the whole map. So base-rarity Anchor is actually the thing that makes firing Vortex yourself controllable at all - a real prerequisite for Vortex being usable, not incidental. The genuinely open question is narrower than first thought: the 50% pull-reduction/25% damage-reduction against an enemy's Vortex only applies at Mythic (`vortex_pull_protection_ratio`/`vortex_damage_reduction`, added 2026-07-26) - whether that enemy-side defense should extend to lower rarities too is still a real balance call, just a smaller one than originally framed. Not changed yet - needs a decision, not a fix.
- **Player mech visual identity gap (found via code 2026-07-27, ties to the "factions" question):** the player's own mech is HARD-CODED to one fixed look regardless of build - `MechRenderer.gd:97`'s `_get_role_visual_profile("player" if is_hero else role)` always uses the fixed "player" profile (bright red, 1.05 scale, "hero" accent) for the human player specifically, completely ignoring `combat_role` even though every enemy role (sniper/brawler/scout/etc.) gets its own distinct palette+accent. Two more contributing factors: (1) a component's outer hex-grid SILHOUETTE is generated once when that component is first created and stays fixed as long as you keep it - swapping individual TILES into the same hex slots only changes the bulk/sleek stretch axis (via `_compute_visual_factors`, keyed on tile TYPE categories present) and conduit glow accents, not the base shape, so reusing the same torso/arm/leg reads as "no visual change" even across very different loadouts; (2) Corporate Sponsorship brand tiles are tagged with `brand_id` (`BrandTileFactory.gd`'s own comment calls out a planned "dark-blue/logo render override") but `MechRenderer.gd` has zero references to `brand_id` anywhere - that override was never actually built. Real foundation exists for a "factions = different assembly/render schemes" feature (role-based profile table already exists and already varies per-role for every non-player mech) - it's just never wired up for the player, and the brand-tile visual hook was stubbed out and abandoned. Not fixed yet - needs a design decision on how player visual identity should vary (by chosen role? by dominant brand affiliation? by build composition?) before implementing.
- **Near-peer difficulty watch:** evolution now earns premiums for player/ally damage and killing blows - watch whether it needs a counterweight (rubber-banding, bounty rewards for the player) as it sharpens.
- **HUD & UX:** damage-number noise (popup budget shipped), unify menu keys (shipped), HUD legibility (shipped). Extend Synergy Codex to cover biome interactions still open.
- **Scout torso shape:** a Scout-role torso should be leg-shaped - 3 hexes wide, variable length (matching the existing leg silhouette code) - rather than the current symmetric-disc torso shared by every role. Note: the recent torso-link-reachability fix deliberately removed the OLD scout/brawler torso thinning (it broke peripheral-link routing); this is a from-scratch replacement silhouette, not a revert, and must keep the 6-neighbor core hub + spoke-tip link placement guarantee that fix added.
- ~~War Room "recent deaths" panel~~ - **already shipped**, found already-built during 2026-07-27 audit: `WarRoomMenu.gd`'s Death Log tab (`_build_death_log`) shows every recorded death (wave/attacker/timestamp) reading `SaveManager.death_log` (rolling, capped at 20, save/load round-tripped). Not gated on garage-entry-from-death specifically (shows full history any time War Room is open) - arguably more useful than the originally-scoped version. Removed from backlog.
- ~~Test Range: HP override field~~ - **shipped 2026-07-27**: `GarageTestRange.gd` now has a "Dummy HP" text field next to Reset Dummy; blank falls back to the old default (1e12).
- **Elite Four rival tier (dialogue written, not wired up - captured/expanded 2026-07-22):** named rivals one tier above the 15 Regulars, open-ended despite the name (5 already: Hrothgar, Dan, Evan, Joe, Frank) - each carries a loose D&D-class flavor (Hrothgar = wizard, huge AoE mortars/fireballs, escalates through the fight; Dan = barbarian, starter gear + pure aggression, wins on fundamentals not power; Evan = rogue, deliberately under-powers shots to stay under a sensor-sweep threshold, first face of the dystopian-hints thread; Joe = uses parts in unexpected ways, gimmick genuinely undesigned yet, may get bespoke part geometry as his signature edge; Frank = bard/twist finale, the shop owner himself, adaptively counters whatever the player's been leaning on all game). Full dialogue (gimmick/intro/win/loss/round monologues) is in STORY_SCRIPT.md's new "Elite Four" section.
  - **Moddable roster (not yet designed):** the user wants this tier specifically to be an open, addable-to roster via modding - "elite enemies" as a mod-extensible category, not a fixed hardcoded 4/5. Naturally pairs with the already-planned Modding Phases 3-4 (Future Expansions, below) - would need Elite-tier rivals defined the same data-driven way `TileStatsRegistry`/`res://tiles/<TileType>/stats.json` already does for tiles (a per-rival JSON/resource: gimmick params, dialogue lines, drop table, spawn condition), rather than another hardcoded rival array, specifically so a mod can add a new one without touching engine code.
  - **Spawn/drop/difficulty-cliff mechanic (design only):** Elite-tier rivals only appear at very late, specific milestone waves (proposed: 1000, 1100, 1200, 1300, 1400...) and drop "absurdly good" unique components when beaten. Deliberate design tension: AFTER an Elite fight, normal wave scaling stops being enough to protect the player - regular enemies can crush a player who doesn't have (or hasn't built around) the Elite-drop parts, and the Director specifically evolves counters to whatever the player built with them even once they do. Needs real design work on exactly how "scaling goes out the window" is implemented (a difficulty curve change, not just bigger numbers) before this is buildable.
  - Still needs, regardless of the above: an unlock gate (after all 15 Regulars beaten at least once) and real gimmick implementations wired into whatever system drives the existing Regulars' mechanical behavior - dialogue alone doesn't make any of them playable.
- **More "transparent" hex tiles (captured 2026-07-22, design deepened 2026-07-27):** the tile roster is short on simple, low-commitment structural options - Directional Conduit is the only real "does basically nothing, just routes energy" tile, Amplifier is rare, and a player who doesn't want to commit a cell to Infusion or Catalyst's bigger effects has few other low-key choices. Candidate additions (all buildable with the existing tile/stats.json pattern, no new architecture needed): Structural Strut (zero energy logic, pure hex-bridging filler - the literal floor of commitment), Buffer Cell (holds one packet one tick then releases unchanged - timing control, no power), Passive Tap (copies a packet to a second direction at full magnitude, unlike Splitter's ratio-based split), Weak Mirror (stripped-down Reflector - redirect only, no damage/defense upside), Insulator (small flat overcharge-instability reduction, doesn't touch the intentional energy-loop mechanic itself).
  - **Circuit-logic angle (raised 2026-07-27, design-only):** rather than (or alongside) pure filler tiles, the user is weighing whether some of these should carry actual player-configurable LOGIC - e.g. gating/timing based on Sync state - which would make Resonators (currently a mid-tier amplify tile) much more central/foundational to mech design than they are now, since Sync would become a value worth deliberately engineering around rather than an amplify-and-forget stat. Open question: is the payoff (deeper build expression, "circuit design" as its own skill) worth the complexity cost against the "low-commitment filler" goal these tiles were originally meant to solve - the two goals may want different tiles rather than one shared solution. Needs a real design pass before any of this is buildable; not scoped yet.
- **Frank's Challenges - modifier ideas (design only, captured 2026-07-22):** two candidate scenario modifiers for the Tactical Puzzle Challenges feature (Execution Queue item 5) -
  1. **Flat damage reduction on enemies:** a straightforward stat gimmick, but a meaningful one - it specifically punishes loadouts built around many weak/spammed projectiles (each hit gets flattened toward zero) rather than a few heavy hits, forcing a shift toward big single-payload "megashot" builds to clear the floor. Good anti-synergy pressure against the opposite playstyle from Rudy's overclocking gimmick.
  2. **Per-projectile damage GATING ("The Eye"):** the inverse idea - an enemy that punishes HIGH power instead of rewarding it. In-fiction justified as a Neighborhood Watch sensor sweep rather than a raw number: on patrol nights, full-power weapon signatures interfere with its sensors, so it takes bonus damage (or is only vulnerable at all) from LOW-power hits and shrugs off/reflects anything over a threshold - forces restrained, low-magnitude builds instead of alpha-strike ones. First concrete hook for the dystopian-hints worldbuilding thread (see STORY_SCRIPT.md) - the challenge's flavor text is where "unauthorized electronics/thermal signature sweeps" gets introduced to the player.

### AAA Polish Roadmap (captured 2026-08-05, user-proposed, not started)
User proposed a 12-item AAA-polish pass across 5 self-named pillars (Audio, Lighting/VFX, Camera Juice, Diegetic UI, Metagame/Immersion Framing), each item pairing a AAA goal with a specific "performance-first" Godot-native implementation technique (native `AudioBus` sidechaining, a single fullscreen shader pass, `MultiMeshInstance2D`, `GPUParticles2D`, native `Tween`) rather than a hand-rolled GDScript equivalent. Agreed as a real list - sequenced below by actual priority given this session's own perf findings, not the pillars' original order.

- **Priority 1 - GPUParticles2D migration** (Pillar II item 6, "High-Density Micro-VFX"): the codebase is not yet all-GPU - 12 `CPUParticles2D` call sites remain across 7 files (`JumpjetResidue.gd`, `DestructibleObstacle.gd`, `RuinObstacle.gd`, `Mech.gd`'s jumpjet trail, `MechRenderer.gd`'s per-mech part sparkle, `FireTrail2D.gd`) against exactly one existing `GPUParticles2D` precedent (`DeathExplosion.gd`). Every fps collapse chased this session (Orbiting Array, Lance residue, obstacle density) traced back to the identical "many small per-instance CPU costs" pattern - this is the same fix class, just applied to rendering instead of physics/collision. Real, scoped, moderate-size migration - the single highest-leverage item on this list.
- **Priority 2 - Tabletop Diorama Shader** (Pillar II item 3, tilt-shift/vignette): a single fullscreen `ColorRect` + `screen_texture` shader, flat GPU cost regardless of scene complexity, and directly reinforces the locked "20mm miniatures on a 4x8ft tabletop mat" fantasy (Section 1) rather than being purely cosmetic. Cheap, on-theme, no architecture conflict.
- **Everything else, unsequenced, grouped by pillar:**
  - **Pillar I - Audio Tactility:** (1) Dynamic ducking via `AudioBus`/`AudioEffectCompressor` sidechain (BGM bus ducks under a HeavyImpact bus) - native engine feature, zero main-thread cost. (2) Layered/pooled weapon audio - `AudioStreamRandomizer` for layering instead of stacking multiple `AudioStreamPlayer2D`s per shot, a 32-slot pre-allocated pool with silent voice-drop past capacity. No baseline weapon-SFX or audio-pooling system exists yet to build on - may be premature until that exists.
  - **Pillar II - Lighting/VFX (remainder):** (4) `WorldEnvironment` Glow/bloom (additive blend, threshold >1.0, HDR `modulate`/`self_modulate` on hot VFX) - real GPU post-process cost, worth budgeting carefully given this session's perf focus. (5) `MultiMeshInstance2D` scorch-mark decals (circular-buffer index into one MultiMesh, hundreds of marks for one draw call) - no existing decal system, clean thematic fit alongside `DestructibleObstacle`'s collapse visuals.
  - **Pillar III - Camera Juice:** (7) Hitstop via a `HitstopManager` autoload scaling `Engine.time_scale` - real caution: this codebase already has many delta-based timers (`WILD_DESPAWN_TIME`, `DAMAGE_TICK_INTERVAL`, weapon `cooldown_timer`s) that would all freeze too unless explicitly exempted (`process_always`) - needs an audit pass of what should/shouldn't respect `time_scale`, not a drop-in. (8) Camera kick/velocity lead in a custom `Camera2D._process()` - low risk, one of the few legitimate per-frame `_process()` use cases in this codebase's own established style.
  - **Pillar IV - Diegetic UI:** (9) `Tween`-based UI panel transitions - already an established pattern here (`LanceBeam`'s fade tween, `DestructibleObstacle`'s debris, `GarageMenu`'s Mythic shimmer) - a low-risk extension of an existing convention, not a new one. (10) `Line2D` + `TIME`-scrolling shader for Garage energy-conduit visualization - pairs well with the existing Simulation Timeline Scrubber/Packet Inspector debug tooling's spirit, applied to normal play instead of just debug view.
  - **Pillar V - Metagame Framing:** (11) Async drop-pod scene transition via `ResourceLoader.load_threaded_request()` - likely architecture mismatch: the Garage currently reads as a same-scene overlay (`get_tree().paused` toggling in `_close_garage()`), not a separate scene requiring threaded loading - confirm the actual scene structure before building it this way; the drop-pod VISUAL is still a good idea, likely achievable as a pure overlay animation without the async-loading machinery. (12) Hobby-shop diorama main menu background - mostly an art/asset production task, low technical risk, matches the existing "Frank's hobby shop" lore already in Section 1.

### Future Expansions
- **Synchronous Online Multiplayer (GodotSteam):** P2P lobby punch-through using GodotSteam (no dedicated servers required).
  - *Network Model:* State-sync/Server-Authoritative with Client Prediction (since the combat/mat phase is explicitly non-deterministic by design; only the Garage hex-routing is deterministic).
  - *Game Modes:* 1v1 duels, 2v2 brawls, 1+1 / 2+2 Co-op wave survival.
  - *Asymmetric PvP (2v1):* A point-buy system for components before the match where the solo player receives twice the point budget as the duo players.
- **Expedition Map:** Slay-the-Spire style branching Tournament Bracket/Shop Campaign.
- **Pilot Skill Tree:** Earn XP for permanent QoL/utility passives across runs (QoL, not power, per ruling).
- **Deployables & Superweapons:** Proximity mines, turrets, massive multi-hex superweapons (e.g. AoE shield domes).
- **Modding (Phases 3-4):** Custom bot types and custom tile scripts extending `HexTile` (staged by queue item 1).
- **Cutscene content:** the framework is shipped and data-driven (`config/cutscenes/`) - actual story beats and real sprite PNGs are authoring work, gated on STORY_SCRIPT.md.

---

## 4. Recently Shipped (orientation only - cleared from tracking)
- **Player mech paint variation (2026-08-05, per the user: "make the player's mech visually vary" + eventually a Paint Rack/sponsor stickers):** every player mech has looked identical since forever - `MechRenderer._get_role_visual_profile()`'s `"player"` branch hardcoded `Color(0.85, 0.15, 0.15)` (bright red) for every hero mech regardless of anything, and `Mech.gd`'s `visual_seed` randomization (`if not is_player: visual_seed = randi()`) explicitly skipped the player, which is why enemy grunts get palette jitter but the hero never did. Added `Mech.paint_color: Color` (alpha 0 = "not set" = falls back to the old hardcoded red) and a matching override in `MechRenderer._rebuild_visuals()` right where the profile is built, mirroring the existing `is_boss` darken-the-color precedent a few lines below it. `Main._setup_player()` now rolls a color from a new curated `PLAYER_PAINT_PALETTE` (10 hand-picked hero colors, not raw random RGB - the same list the Paint Rack will offer as swatches) for a brand-new game or an old save that predates this feature, and persists the choice (`Main.player_paint_color`, a hex string - JSON saves have no native Color type) through `SaveManager.save_game`/`load_game` the same way `player_sponsorship` already does. Added `Mech.refresh_visuals()` (generic sibling of the existing `refresh_boss_visuals()`) as the hook a future "apply paint" action will call. This is the foundation only - no Garage UI yet; the Paint Rack tab and sponsor-branded stickers (tying into the existing but currently tile-only `BrandRegistry`) are a follow-up. Bumped to 1.1.8.0.
- **Wave-65/76 stuck bug, real root cause found + crash from user-supplied console log (2026-08-05):** the user made it past wave 65 on v1.1.7.6's watchdog, then hit wave 76 stuck again plus an actual engine crash (signal 11) - this time they sent real Godot console output, which the two prior blind rounds never had. It showed the actual bug: `LootManager.gd:94` called `comp.get("equipped_chips", [])` on a `ComponentEquipment` Node - `Object.get()` only ever takes 1 argument (the 2-arg default-value form is a `Dictionary.get()`-only feature, not a general Node one), so this threw "Invalid call to function 'get' ... Expected 1 argument(s)" on EVERY mech death (any mech with a non-null component), logged repeatedly right up to the crash. Fixed by using the already-typed `comp.equipped_chips` property directly (matches how the rest of this same function already accesses `comp.has_node(...)`/`comp.get_node(...)` on the same objects, and matches the 1-arg `comp.get("x")` pattern already used correctly everywhere else in the codebase - `LootManager.gd:94` was the only 2-arg offender). Traced whether this was really the wave-65/76 stuck root cause by reading `Mech.die()`'s call order: `LootManager.generate_loot_for_mech()` fires BEFORE `died.emit()`, so this repeated hard error was corrupting the death sequence's ability to reliably fire the wave-clear signal - a much better explanation than the `_start_wave()` re-entrancy race from the two prior rounds. Also fixed a real side effect of the v1.1.7.5/6 watchdog itself: forcing a bare `_start_wave()` retry left the previous wedged wave's still-alive mechs wandering the map with `died` still connected - their later deaths decremented the freshly-reset counter for a wave they were never part of, observed dragging `active_enemies` to -24 by wave 76. Added `_clear_stale_wave_enemies()` (disconnects every `died` listener - not just `_on_enemy_died`, since bosses/rivals/champions route through their own handler which calls it internally - then queue_frees and resets `active_enemies` to 0, same pattern `_on_player_died`'s Garage-kick-back already used) and wired it into both watchdog recovery points. The engine crash itself (segfault, no debug symbols in the release build) couldn't be root-caused with certainty from this backtrace alone, but removing a script error that fired on effectively every enemy death in the game is a real fix regardless and a very plausible contributor. Bumped to 1.1.7.7.
- **Wave-65 stuck-forever bug, round 2 - watchdog/self-heal for a wedged `_spawning_wave` (2026-08-05):** v1.1.7.5's `_start_wave()` re-entrancy guard shipped and the user was STILL stuck - the F3 overlay it added (see below) proved decisive here: it showed `active_enemies 0, spawning true`, permanently. That means `_spawn_wave_async`'s awaited chain (`director.spawn_squad` -> `_assemble_squad`) hit a runtime script error partway through on wave 65's regular-squad spawn (the boss itself, spawned synchronously and separately by `_spawn_boss`, was unaffected and had already died) - GDScript has no unwind/finally, so the coroutine just halted and never reached its own `_spawning_wave = false` line, wedging the new re-entrancy guard shut. Worse than before in one way: the user's own workaround (cycling Garage to force fresh spawns) no longer worked either, since the guard now refused to start a new wave at all. Root-causing the actual script error needs real console/log output this sandbox doesn't have access to (asked the user for `user://logs/godot.log` or a terminal-launched debug build) - in the meantime, made the stuck state impossible to get wedged in permanently: `_spawning_wave_started_at` (`Time.get_ticks_msec()`) is recorded alongside the flag, and both `_start_wave()`'s guard and a new proactive check in `_process()` treat a flag that's been true for 10s+ (well beyond the ~2-3s a legitimate 50-squad-max spawn should ever take) as proof it's wedged rather than still working - force-clear it and retry `_start_wave()` automatically, without requiring the player to walk back to Garage first. Bumped to 1.1.7.6.
- **Wave-65 stuck-forever bug, real root cause found - `_start_wave()` re-entrancy race + F3 diagnostic overlay fields (2026-08-05):** user report - still stuck on wave 65 after BOTH prior fixes below (wild-bot counter leak, then its own double-decrement revert) landed; neither actually resolved it, meaning both had been treating a symptom, not the cause. Re-reading `Main.gd`'s full wave-start/extraction/garage cycle surfaced a real, previously-unexamined race: extraction is entirely player-voluntary and NOT gated on the current wave having cleared - `garage_timer` (90s) counts down independent of `active_enemies`/wave state, and walking into the `ExtractionMarker` calls `_open_garage()` directly. `_close_garage()` re-triggers `_start_wave()` for the same wave (never incremented, since it never cleared) via a 5s countdown - but `_start_wave()` calls `_spawn_wave_async(...)` WITHOUT awaiting it, so it returns immediately while the async spawn (staggered one squad per 0.12s beat) keeps running for real wall-clock seconds afterward. A player repeatedly extracting/redeploying while stuck (exactly what "if I go to garage and come out enemies will spawn" describes) could re-trigger `_start_wave()` while a PREVIOUS call's spawn loop was still mid-flight - the second call resets `active_enemies = 0` and re-runs the boss dispatch (spawning a SECOND boss on wave 65, a `% 5` boss wave) while the first call's loop is still incrementing `active_enemies` from what it thinks is a consistent baseline, with zero mutual exclusion between the two. Fixed with a re-entrancy guard at the top of `_start_wave()` (`if _spawning_wave: return`) - safe against deadlock since `_spawning_wave` is bounded by the existing `safety_break` loop and always eventually clears on its own. Also added `active_enemies`/`_spawning_wave`/`current_wave` to the F3 perf overlay (`FpsCounter.gd`, new `wave_state_label` line) - two prior fixes were both diagnosed blind from visual symptoms alone; if this one still doesn't fully resolve it, the next screenshot will show the actual state instead of requiring another round of code-reading-as-guesswork.
- **Wave-clear counter fix + active wild-bot reinforcement (2026-08-05):** user report - stuck on wave 65 indefinitely; leaving and returning from the Garage spawned fresh enemies, but killing all of them never advanced the wave, even after a full app relaunch (ruling out a simple "accumulated leak over many waves" theory). Root cause: `_finish_flee()` already hands a fleeing bot's wave slot back to `Main.active_enemies` the MOMENT it goes wild (disconnects `died`->`_on_enemy_died` and manually fires it once - "a survivor hiding at the map edge must never stall the wave-clear," pre-existing code). v1.1.7.3's `despawned_wild` signal fired a SECOND decrement for the SAME bot when it later timed out, double-counting and wedging `active_enemies` negative - worse than the original bug, since a boss wave (every 5th - wave 65 qualifies) runs long enough for a fled bot to hit `WILD_DESPAWN_TIME` mid-fight on a completely fresh session, no accumulated history required. Reverted `despawned_wild` entirely (signal, emission, both `Main.gd` connections). Also hardened `Main._on_enemy_died`'s wave-clear check from `== 0` to `<= 0` so it self-heals against any future/residual drift instead of getting permanently wedged by one bad decrement.
  - **Replaced with active wild-bot reinforcement (user-designed):** rather than passively waiting up to `WILD_DESPAWN_TIME` (60s) to be picked up by the next brand-new squad assembly that happens to want its exact role, a wild bot now asks `SquadDirector.try_reassign_wild_bot()` every ~1s for the nearest squad ALREADY on the field with an opening in any role ("hop into any free role, whatever it is") - no combat_role match required, unlike the existing spawn-time `_assemble_squad` recruitment path (left unchanged, still the fallback for squads still being freshly formed). If nothing has an opening, once the bot has been wild for a short grace period (`REASSIGN_GRACE_PERIOD`, "after a brief retreat") it just joins the nearest active squad outright, capped at the same `MAX_SQUAD_SIZE`=12 ceiling `_merge_squads` already uses. `WILD_DESPAWN_TIME` remains as the fallback only for the genuinely rare case of no active squad existing to join at all. Should make the timeout-despawn path (and therefore this whole class of counter bug) much rarer in practice, independent of the counter fix above.
- **Obstacle density cap + edge-treeline/copse placement rework (2026-08-05):** user report - obstacle density was confirmed as a real perf lead (an existing `DestructibleObstaclePerfCheck.gd` comment already admitted dense biomes can generate thousands of individual physics-body obstacle nodes) and, separately, was "mostly a nuisance" to actually play around. `MapGenerator._should_spawn_obstacle` rewritten: per-biome density values cut to roughly half-to-60% of the old tendril-band chances (direct cap), and the placement SHAPE changed from one wide "tendril" noise band (a thin winding zero-crossing line that could run most of the map's width) to two independent, sparser rules - an edge treeline (only within `EDGE_MARGIN_TILES` of a border, and only where a coarse noise sample says that STRETCH of edge is treed, so most of the perimeter stays open) and interior copses (a tight high-value noise threshold picking out small isolated blob clusters instead of long connected lines). Normal map mode keeps its own earlier "gaps need to be larger" ruling as a further density multiplier. Untested in a live session (no Godot runtime in this environment) - exact thresholds/percentages are a first-pass estimate, flag for an in-game density check.
- **Missile Rack: lead-prediction damage fix, Hunter/AOE firing modes, lance_mounts crash fix, tooltip overflow fix (2026-08-05):** four fixes to the same tile, consolidated. (1) *Damage fix* - "missiles weren't doing much damage": `MissileRackTile._fire_combined_projectile` picked its own target autonomously (the FURTHEST in-range enemy, no player aim compensation possible, unlike the player-aimed Mythic Mortar pattern) and fired every shell at that target's static current position; with a shell flight time up to 2.2s and the target being exactly the enemy most likely to be actively repositioning, shots very plausibly landed on empty ground past the ~40px splash floor by the time they arrived. Added single-pass lead prediction: estimate flight time from the target's current position, then extrapolate its landing-time position using its own `CharacterBody2D.velocity` before computing the salvo's spread. (2) *Firing modes* (user-designed) - Mythic Missile Racks now get a `mythic_mode` toggle in the Garage tile config popup (same generic Mythic mode-cycle convention as Jumpjet/Directional Conduit/Shield Generator/Actuator - `GarageTileConfigPopup.gd`), persisted for free through `SaveManager`'s existing generic mythic-prop sweep. Hunter (default, all rarities) is the pre-existing salvo unchanged - several shells, all aimed at the one furthest-in-range target. AOE (Mythic only) fires a single wide burst instead: `MortarShell.gd` gained an opt-in `equal_split_all_victims` detonation mode (every other caller unaffected) that finds every valid target within blast radius and splits the FULL undivided salvo damage budget equally across them, each still driven through the real `Projectile._handle_hit()` pipeline (not a flat `apply_damage`) so elemental statuses/procs land properly per victim. Blast radius layers a new `radius_mult` on top of the existing shared `explosion_radius_for()` formula (unchanged for every other caller) - AOE mode starts wide and grows EXPONENTIALLY with Explosion synergy investment (`AOE_BASE_RADIUS_MULT * exp(AOE_EXPLOSION_EXP_K * r_explosion)`), a first-pass tuning not yet live-verified. (3) *Crash fix* - user-reported repeating script error, "Invalid access to property or key 'cooldown_timer' on a base object of type 'Resource (MissileRackTile)'" at `Mech._tick_weapon_charges`. Root cause: `Mech.gd`'s tile-collection pass (`_collect_weapon_mounts_and_tile_capabilities`) had `tile.tile_type == "Missile Rack"` folded into the same OR chain as Lance Mount/Orbiting Array, pushing every equipped Missile Rack into `lance_mounts` - but unlike those two, `MissileRackTile` has no `cooldown_timer`/`ready_to_fire`/`fire()` at all (it already fires through the ordinary `pending_packets`/`precalculated_weapons` pipeline shared with a plain Weapon Mount, per its own file header). Removed Missile Rack from that OR chain - it was never supposed to be there. (4) *Tooltip overflow* - Missile Rack's long blurb ran off the right edge of the window: `garage.tooltip_label` (`GarageUIBuilder.gd`) had no fixed width or autowrap and `on_tooltip_requested` (`GarageInventoryPanel.gd`) positioned it at a raw mouse-relative offset with no viewport clamp, the same overflow bug class `FpsCounter.gd`'s overlay hit before its own fix. Gave the label a 320px `custom_minimum_size` + word-wrap, and clamped its position against the viewport using its real post-text `get_minimum_size()`.
- **JumpjetResidue perf fix - Lance damage-trail was still tanking FPS (2026-08-05):** live playtest report at wave 57 - 3fps/133ms with only 41 enemies/2 drones alive, screenshot showing a long visible dotted damage-residue trail (LanceBeam's signature "cooling lava" chain). `JumpjetResidue.gd` (the hazard-zone class LanceBeam/PlayerController's jumpjet trail/BossBrain's Incinerator ability all reuse) was still `extends Area2D` - a real physics-server body registered every physics tick for every live zone, regardless of the existing 10Hz damage-query throttle (that throttle only cut the *query* rate, not the broadphase registration cost). A single Lance shot can drop dozens of these at once, each living up to 25s, so a Lance-heavy fight steady-states at ~200 concurrent real physics bodies - the same cost category `OrbitingProjectile.gd` was already converted away from the day before. Converted `JumpjetResidue` to `extends Node2D` with damage now read from the same throttled `EntityCache` group snapshot every other hot-path scan in this codebase uses, plus a plain distance-to-radius check standing in for the old circle-overlap query (equivalent for this shape, no physics-server round-trip at all). `collision_mask` (4=Enemies/8=Player) replaced with an explicit `by_player: bool` on all 3 call sites (LanceBeam's residue chain, BossBrain's Incinerator drop, PlayerController's own jumpjet trail).
- **Lance Mount: aim fix, visual tuning, F3 overlay build-version tag (2026-08-05):** three changes to the same weapon/overlay, consolidated. (1) *Aim fix* - user report: "I can't seem to meaningfully aim the lance," confirmed via a screenshot showing the beam firing off toward empty space far from any enemy. Root cause: `Mech.last_aim_position` (what `LanceMountTile.fire()` reads when its cooldown clears and it auto-fires) was only ever written inside `_shoot_impl()`, which for the player only runs while a fire button is actually held (`PlayerController.handle_input`) - release fire to reposition and it froze at wherever you last shot, so the Lance's next auto-fire (up to 10s later) beamed off in a stale direction with no way to aim it in the moment. Fixed by tracking the live cursor unconditionally every frame in `PlayerController.handle_input`, independent of firing state. (2) *Visual tuning* (user report: "the lance puddle residue can fade faster, the beam should fade slower, and be wider") - `LanceMountTile`'s `residue_lifetime` stat cut 25s -> 12s (also trims concurrent hazard-node count, complementing the JumpjetResidue perf fix above), `LanceBeam.BEAM_LINGER_TIME` raised 3.0 -> 5.0, and every beam/flare draw layer widened ~1.4x. (3) *F3 version tag* - added a small dim version-tag line to the F3 perf overlay (`FpsCounter.gd`) reading `v<project version> (<git short hash>)` - `project.godot` now carries a `config/version` bumped per release - so a bug-report screenshot says which build produced it without a follow-up question.
- **Evolving stock builds + wave archetype shaping (2026-08-05):** enemy loadout generation was a real spawn-time perf cost (AutoEquipSolver's BFS/placement algorithm re-run 3x per mech, every mech, every wave) and, separately, every bot independently rerolled its own loadout with no memory - two different problems solved together. `AutoEquipSolver.gd` now caches its placement decision per (component shape, rarity, inventory composition) and replays it on repeats (pure perf win, zero behavior change on a cache hit's topology). On top of that, a new `StockBuild`/`StockBuildMutator`/`StockBuildEvolution` trio (`scripts/ai/`) gives each (squad template, role) its own evolving loadout - mirrors the existing `SquadTemplate`/`SolverProfile`/`BossProfile` evolution pattern exactly, but checkpoint-driven (Garage visit, or a tracked-deviation batch filling up) rather than per-death, since a full concrete tile layout is heavier to evaluate than an abstract weight. Persists through the same `SquadProfileManager` save file and clipboard export/import as the other three evolving pools (bumped to v1.4). `Main._start_wave` now also shapes wave composition directly - a role-heavy wave every 4th, scout-heavy every 3rd, a new "Gang Up" event every 7th restricting spawns to the 3 templates with the best `SquadTemplate.recent_fitness_ema` (a new fast-decaying recency signal, separate from the existing lifetime `spawn_weight`) - both a real difficulty texture change and a further cut on how many distinct builds a heavy wave needs solved at once. Squad member spawning is also staggered one frame apart per member (`SquadDirector._assemble_squad`) on top of the existing per-squad 0.12s beat, smoothing whatever solver cost remains. No headless regression check exists yet for `AutoEquipSolver` specifically - worth adding one before this gets touched again. Merged into master 2026-08-05 alongside an independently-developed OrbitingProjectile perf/correctness fix (below, from a separate tool/session) - reconciled by hand: kept that fix's Area2D->Node2D rewrite as the base and re-applied this branch's ProjectileManager saturation-registration/pause-survival integration on top of it (see OrbitingProjectile.gd).
- **OrbitingProjectile perf + correctness fix (2026-08-04):** user report - 5fps/133.1ms at wave 6 with only 6 live enemies, screenshot showing a Missile Rack salvo + several Orbiting Array orbs live at once ("still running into (MUCH less bad) slowdowns with a more built mech"). Root cause: `OrbitingProjectile.gd` (the Orbiting Array capital weapon's orbiting shots) was built `extends Area2D` - a real per-orb physics body (CollisionShape2D, collision_layer/mask, area_entered/body_entered monitoring) registered with the physics server every frame, reintroducing the exact per-projectile overhead the earlier Rust Phase 3 cutover specifically eliminated from `Projectile.gd` (which now extends Node2D + reports to the batched `ProjectileBroadphase`). Worse, the Lightning-synergy orbit ran a raw `PhysicsShapeQueryParameters2D.intersect_shape()` call every single frame, unthrottled. Since `OrbitingProjectile` is a wholly separate script from `Projectile`, none of this cost ever showed up in any `FpsCounter` breakdown line - a genuinely invisible bottleneck. Fixed: `OrbitingProjectile` now `extends Node2D`, with contact damage and the lightning lash both moved to throttled (10Hz / 5Hz) `EntityCache` group distance-checks instead of continuous physics monitoring - same pattern `Projectile.gd`'s homing-target throttling already established (task #33). Also found and fixed two real, separate correctness bugs while rewriting this file: `setup()` called `EnergyPacket.get_dominant_synergy(synergies)` as if it were a static function taking a Dictionary - it's an instance method taking none - so `dominant_synergy` silently stayed stuck at RAW and orbiting shots never got their real elemental orbit pattern/color/lash (fixed by adding a genuine `static func dominant_synergy_of(synergies: Dictionary)` to `EnergyPacket.gd`, with the instance method now delegating to it); and the old `body_entered` handler called the nonexistent `EnergyPacket.synergy_name()` (real name: `element_name()`), meaning orb contact damage had likely never actually landed in production. Deliberately did NOT add `OrbitingProjectile` to the shared `"projectile"` group when restoring damage logic - `MagnetSystem.gd`'s repel-mode loop treats every group member as a real `Projectile` and mutates fields (`fired_by_player`, `collision_mask`, `direction`) this class doesn't have; today's property-name mismatch (`by_player` vs `fired_by_player`) makes that a silent no-op, but joining the group would make it a live footgun the moment either script's field names change. Also deliberately did NOT restore the old `area_entered`-based "intercept incoming enemy projectiles" mechanic - it had already been fully dead code since Projectile.gd stopped being an Area2D (an Area2D's `area_entered` never fires for a plain Node2D), so reviving it as a live gameplay behavior change was treated as out of scope for a performance fix. Verified via new `OrbitingProjectileCheck.gd` (8 assertions: dominant_synergy resolves correctly, no Area2D dependency, not in the "projectile" group, throttled contact damage lands with the correct amount/element, throttled lightning lash lands) plus `CapitalWeaponFaceKeyCheck`/`TestRangeCheck`/`TileConfigCheck`/`HitDecayCheck` all re-run clean.
- **Chip equip picker (2026-08-04):** user ask - equipping a Mod Chip always silently grabbed whichever chip was first in the pool ("next one up to bat"), no way to choose. `TileActionMenu.equip_chip()` split into `equip_chip_at(index)` (the real primitive, now used by both paths) and a thin `equip_chip()` wrapper (`equip_chip_at(0)`, kept for existing debug-check call sites/backward compat) plus a new `open_equip_chip_popup()` that the real "Equip Mod Chip" button now opens instead - same pill-row/type-filter/stat-filter picker UI as the Chip Splicing picker below (single-select instead of select-2), commits via `equip_chip_at()` on the chosen index.
- **Chip Splicing picker UI polish (2026-08-04):** user feedback on the initial Splice Chips popup - too narrow (chip names clipped), no way to filter a growing pool, chip descriptions unreadable as a wall of text. Popup widened 400x420 -> 700x520; added a type filter (All/Plain/Corrupted) and 11 per-stat filter buttons (OR-narrows the list to chips containing any selected stat - useful for hunting a Tier 2 shared-stat match); replaced each chip's `_describe_chip()` text row with a strip of small colored "pills" (`STAT +13%` in the stat's own color) reusing `EnergyPacket.get_color_for_synergy()`'s existing per-element color table (dmg_mult/spd_mult, not elemental, get two fixed colors of their own) - full names still available as a hover tooltip. A `✦` mark replaces the `[Corrupted] ` text prefix.
- **Obstacle/terrain color contrast for accessibility (2026-08-03):** user report ("NOT accessible") plus direct visual confirmation via the same stress-test video (dark-green `TreeObstacle`/`Cactus` obstacles nearly invisible against grassland/forest's own green biome palette). Added a luminance-contrasted `Line2D` outline (light border on dark fills, dark border on light fills) to `TreeObstacle.gd` and every `DestructibleObstacle.gd` type (Boulder/Cactus/IceBoulder/LavaRock/StoneWall) - reads against any biome's palette without needing per-biome color-matching logic. `RuinObstacle.gd` not touched (its architectural grey/red/tan palette doesn't share hue with any biome, lower priority) - candidate for the same treatment later if it turns out to need it.
- **Full-codebase stability sweep (2026-07-27):** 5-way parallel audit across core/entities, AI/combat/hazards, all 32 tiles, UI/visuals/debug, and the Rust extension, followed by fixes for every confirmed bug found: `MissileRackTile.gd` missing its `class_name`/`extends` header (wouldn't compile), Lance Beam's `check_face_gate()` never being called before `clear_pending()` wiped its state (the weapon could never fire in real gameplay), a double death-report panel on player death (`Main.gd`), a squad-merge signal-leak double-counting damage telemetry into two squads at once (`SquadDirector._merge_squads`), missing `reset_simulation_state()` overrides on Lance Mount/Orbiting Array/Missile Rack/Component Link/Magnet/Jammer Module/Drone Bay (Timeline Scrubber replay determinism), a missing `tiles/OrbitingArrayTile/stats.json`, Settings menu stacking duplicate instances on repeat clicks (MainMenu/PauseMenu), JumpjetResidue picking damage element by presence instead of magnitude, and a family of Rust FFI panics (`.to()` instead of `try_to()` on Variant/Dictionary data from GDScript, a Splitter weights/faces length mismatch, a Lance cell-index silently misattributing to the anchor cell) across `hexgrid_sim.rs`/`part_rasterizer.rs`/`projectile_flight.rs`/`projectile_broadphase.rs` - all rebuilt and verified compiling clean (debug + release). Also corrected two stale doc entries this sweep surfaced: Corporate Sponsorships and the War Room Death Log were both already fully shipped despite still being listed as open backlog. Dead `GarageUI.gd` prototype (never instantiated) deleted. Full findings detail lives in that session's transcript, not restated here - this line exists so a future assessment doesn't need to re-run the same audit.
- v1.1.2 post-release fixfest: the release shipped three non-functional capital weapons - Lance Mount/Orbiting Array had lost the check_face_gate() call that arms them (both could accumulate fed energy but never actually fire), and Orbiting Array separately carried a nonexistent EnergyPacket.clone() call, a footprint-offset .q/.r typo (should be .x/.y), and a Rust-sim KIND_PASS misclassification that let energy pass straight through it on the native sim path. All fixed, plus its 3-hex footprint now actually places as the advertised triangle (was placing as a straight line, Lance-style). Missile Rack (previously a stub) is now a real always-indirect salvo weapon mount. AutoEquipSolver no longer burns Weapon Mounts/Missile Racks as path filler (a real capturing tile used as wire silently ate the packet instead of relaying it - the root cause behind "the autosolver builds don't leverage tiles well") and now aims the movable Accessory Return's output back into the routed network. Garage Test Range didn't list capital weapons at all (checked precalculated_weapons only, never Mech.lance_mounts) - fixed. v1.1.1's dialogue.json content had been silently reverted to the v1.1.0 blob by the same release commit - restored. App/installer icon replaced (was still the stock Godot engine logo, never actually rebranded).
- Easy-wins batch: starter inventory rarity curve (no more free Legendaries), Ambusher decloak reveal burst + targetable Heal Beacons (disable-priority), HUD text outlines + unified menu keys (WarRoomMenu now uses real InputMap actions like everything else), Champion Card sprite portrait (real in-engine MechRenderer render composited above the existing hex-schematic blueprint via a throwaway SubViewport rig)
- Data-driven tiles: all 24 tile types + `TileStatsRegistry` autoload, `res://tiles/<TileType>/stats.json` per type, zero behavior change (verified)
- Simulation Timeline Scrubber + Packet Inspector: deterministic re-run-to-step-N replay (no state buffering - Resonator/Splitter/Catalyst mutable state resets via HexTile.reset_simulation_state before each replay), drag-to-scrub HSlider synced to live auto-play, click-a-tile packet inspector showing current output + last-5-packet history per direction
- Garage Test Range: live-fire any armed mount's real combat packet at a target dummy in a private physics world (SubViewport + own World2D) - real projectiles, patterns, damage numbers, shots/avg readout
- Tile config batch (`1300fb6`): Resonator per-path Sync Dropoff, Mythic Splitter output ratios, Catalyst gated injection (magnitude + cadence gates), Accumulator auto-dump thresholds - all in the click-a-tile config popup, all save-persistent
- Wild-bot flee thresholds: skittish roles break off and go wild (squad + wave accounting stay honest, wounds regen while loitering, director recruits them back)
- Saturation perf tiers: volley consolidation, projectile visual LOD, damage-popup budget (`d65ab79`)
- Drone sortie leash fix (1000/2000px), near-peer AI fitness premiums, Mythic boss torso hex-budget fix (`d65ab79`)
- Pixel-art cutscene framework + sample Frank beat (`6bc1b63`)
- Power-scaled jammer fields + minimap jammer secrecy swirl (`3f3df78`)
- Unlimited named save slots for builds and parts (`fa26417`)
- Earlier: Scrap Shop, captured loadouts, Lance Mount, recon/Chinook drones, lightning blink identity, mortars with true direct-fire payloads, strategic zoom, demo builds, module keybinds, obstacle corridor carving.
